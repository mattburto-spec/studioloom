# Project: Questerra (StudioLoom)

## Instructions for Claude
- Whenever the user types `saveme` on a line by itself, update this CLAUDE.md file AND any other relevant docs in the project (e.g., `docs/roadmap.md`, `docs/ai-systems-audit-2026-03.md`, `docs/architecture.md`, etc.) to reflect the current state of the project — what's done, what's next, any new decisions, architectural changes, or notes. Only update files that are actually affected by what was worked on in the current session. **After saving, also trigger the `refresh-project-dashboard` scheduled task** to update the project dashboard and master CWORK/CLAUDE.md index.
- **When building any interactive toolkit tool or student-facing AI interaction**, read `docs/education-ai-patterns.md` first. It contains the 5 core patterns (effort-gating, Socratic feedback, staged cognitive load, micro-feedback loops, soft gating) that ALL interactive tools must follow. SCAMPER is the reference implementation.
- **When making any design decision or establishing a new pattern**, add it to `docs/design-guidelines.md`. This file feeds the dashboard Guidelines tab and is the canonical reference for all project principles. Format: `### ID. Title` followed by description, source, and status (Documented/In code/Gap).
- **When making decisions about student profiling, measurement, cultural adaptation, language scaffolding, peer grouping, or any feature that touches student identity/wellbeing**, read `docs/research/student-influence-factors.md` first. It contains Hattie-style effect sizes for 24 factors (cultural, linguistic, psychological, environmental) with measurability ratings and interaction effects. This is the research foundation for the Student Learning Profile and Discovery Engine profiling.

## What this is
A gamified AI-powered learning platform for MYP Design students to work through the design cycle with structured scaffolding, Socratic mentoring, and teacher-managed knowledge bases.

## Stack
- Framework: Next.js 15.3.3 (App Router), React 19, TypeScript 5.8
- Styling: Tailwind CSS 4.1
- Backend/DB: Supabase (PostgreSQL 15+ with pgvector), Supabase Auth + custom student token sessions
- AI: Anthropic Claude (Sonnet 4 for generation, Haiku 4.5 for mentoring), Groq + Gemini fallbacks
- Embeddings: Voyage AI voyage-3.5 (1024-dim)
- Hosting/Deploy: Vercel

## Current status
Advanced prototype — core platform functional (unit builder, knowledge base, student experience, design assistant, grading). Operational infrastructure now partially in place (rate limiting, usage tracking, Sentry, Vitest). Design Thinking Toolkit now live at `/toolkit` with 12 interactive AI-powered tools plus 42-tool browsing catalog. Student Toolkit Access spec written (embedded mode + standalone mode + AI suggestions). Planning Tools UX spec ready for build. **AI Intelligence System (18 Mar 2026):** Pass 0 source-aware classification + 5 type-specific analysis pipelines (v2.0.0), Design Teaching Corpus (Layer 1) wired into all generation prompts, learning-based timing model, teacher style profiles with passive signal collection, student Design Assistant upgraded with design teaching intelligence. **Lesson Timing Engine (19 Mar 2026):** Workshop Model enforced in all generation prompts (4-phase: Opening → Mini-Lesson → Work Time → Debrief), 1+age instruction cap rule, usable time always calculated (never raw period), server-side timing validation with auto-repair, Phase Timeline Bar component for teacher modification, timing presets (Balanced/Hands-On/Instruction/Critique), post-lesson timing feedback system, extension generation for early finishers. **Open Studio (20 Mar 2026):** Replaces old Own Time system. Teacher-unlocked self-directed working mode (per student per unit). AI mentor switches from Socratic tutor (guided) to studio critic (Open Studio). 5 interaction types with drift detection + 3-level escalation + auto-revocation. Configurable check-in intervals (5-30 min). Full session lifecycle with activity logging, reflection, and productivity scoring. Fan menu rebuilt with Framer Motion (M3 Expressive style). UI modernized (frosted glass headers, updated criterion colours, teacher/student/admin dashboards refreshed). Admin AI controls redesigned with macro dials (camera-style SVG) + presets + macro/micro toggle. Teacher toolkit page at `/teacher/toolkit`. DesignPlanBoard component built (MYP design cycle kanban). TeachingDNA profile visualization on teacher dashboard. Student Design Assistant auth fixed (was using Supabase Auth, now uses student token sessions). Student lesson hero blocks use dark-to-color gradients. 3-pass analysis viewer added to LessonProfileReview. Teaching DNA Profile feature added to roadmap (Phase 3). **Teaching Mode (20 Mar 2026):** Full teaching cockpit at `/teacher/teach/[unitId]` — 3-column layout (lesson navigator, phase timer + live student grid, notes + extensions). Live student status polling (8-sec interval) with "Needs Help" detection (3+ min inactive). Dark-themed projector view at `/teacher/teach/[unitId]/projector` with phase-aware content display + postMessage sync from dashboard. "Teach This" button on unit detail page. "Teach Now" quick-launch section on teacher dashboard with progress rings. Comprehensive feature audit completed (120 features, 17 categories, 11 wiring findings). Skeleton generation hardened (dynamic max_tokens scaling, detailed error logging for debugging empty AI responses). **New Metrics / Melbourne Metrics (20 Mar 2026):** University of Melbourne competency assessment framework integrated. Student Competency Pulse (3-point self-assessment) + Teacher Observation Snap (4-point). Per-unit NM config via 3-step wizard. Pop art visual identity (hot pink, cyan, electric yellow, bold black borders, halftone dots). NM Results panel on teacher unit detail page. NM button on dashboard unit rows. Per-student NM observation button in Teaching Mode. **Unit-as-Template Architecture (20 Mar 2026):** Units are now content templates assignable to multiple classes. `class_units` junction table stores per-class configuration (NM on/off, timing overrides future). NM config moved from unit-level to per-class settings page. Unit detail page shows "Assigned Classes" section with clickable cards. New class-unit settings page at `/teacher/units/[unitId]/class/[classId]`. All NM API routes read config from `class_units` with fallback to `units.nm_config`. Dashboard filters archived classes. Auth helper at `src/lib/auth/verify-teacher-unit.ts`. **Deployed to Vercel (20 Mar 2026).** **UX Polish Pass (20 Mar 2026):** Skeleton generation auto-retry on truncation (16384 token ceiling), teacher dashboard redesigned (killed vanity KPI cards, compact AttentionBar, collapsible sections), student dashboard redesigned (gradient hero, "Continue Where You Left Off" card, UnitThumbnail with deterministic gradients), unit cards fixed (overflow-hidden thumbnails, better text spacing). Student lesson page: sticky top nav bar with Dashboard button always visible. Reflections redesigned from tickbox to "Pause & Think" with effort-gating (meaningful word count, sentence starters, soft gate before confidence slider). Open Studio JourneyMap made sticky. **Open Studio Comic Strip Discovery (20 Mar 2026):** DiscoveryFlow chat replaced with visual comic strip panels — SVG scene illustrations per step (mountains, compass, forest, spotlight, rocket), illustrated mentor + student characters, thick comic-panel borders, white speech bubbles with tails, cinematic step transition panels with walking characters, inline ProfileReveal cards, spring-physics progressive panel reveals. Side profile panel removed, replaced with inline reveals and bottom project statement bar. **Student Lesson UX Fixes (21 Mar 2026):** Critical text input bug fixed (usePageData creating new object references every render → usePageResponses resetting form state; fixed with useMemo + ref guards). Student feedback simplified to pace-only pulse (🐢👌🏃) — one tap, feeds timing model. Migration 036 applied (lesson_feedback columns nullable). Teacher pace feedback aggregation view on class progress page. **School Calendar / Term System (21 Mar 2026):** Per-teacher academic calendar (terms/semesters/trimesters), term picker on class-unit settings page, modeled after ManageBac/Toddle. Migration 037 NOT YET APPLIED. **Timetable & Scheduling System (21-22 Mar 2026):** Rotating cycle timetable engine (5/6/8/10-day cycles), anchor-based modular arithmetic for cycle day calculation, `school_timetable` + `class_meetings` tables (migration 038 APPLIED), teacher settings UI (cycle config, class meetings, excluded dates), iCal import (URL + file upload) with holiday auto-detection, student next-class API + dashboard display, lesson count on class-unit settings page. Teacher settings restructured: "Workshop & Equipment" split into own tab. **Lesson Scheduling & Overrides (22 Mar 2026):** LessonSchedule component maps unit lessons to real calendar dates using cycle engine + term dates. Teachers can mark lessons as "continues next class" (adds extra sessions), skip specific dates, and add per-lesson notes. Schedule overrides stored as JSONB on `class_units` table (`schedule_overrides` column added in migration 037). TimetableGrid visual component replaces row-by-row class meeting entry — click-to-add/remove in Day×Period grid with color-coded classes. SchoolCalendarSetup template buttons always visible (fixed: were hidden after first save). **Safety Badge System (21 Mar 2026):** Complete workshop safety certification system. Teacher creates badges with learning cards + question pools (5 question types: multiple choice, true/false, scenario, sequence, matching). Teachers assign badges as unit prerequisites ("Require for Unit") or grant directly to students. Students see pending tests on dashboard (amber banner), take 3-screen test flow (learn → quiz → results), auto-scored with pass/fail threshold. Badge gate blocks unit access (server-side on narrative page, client-side on lesson pages) until all required badges earned. Teacher sees results on badge detail page (with student names, scores, pass/fail, class filter) and safety column on class progress page. Dashboard shows amber shield chip with badge count per unit. Migration 035 (safety_badges tables). Retake cooldown + expiry enforcement. Free public tool page at `/tools/safety`. Badge thumbnails system. ~15 files, ~5K LOC. **Unit Forking Architecture (22 Mar 2026):** Copy-on-write content forking for per-class unit editing. When a teacher edits a unit in a class context, content is deep-copied from `units.content_data` into `class_units.content_data`. Resolution chain: `class_units.content_data` (if non-null) → `units.content_data` (fallback). Migration 040 adds `content_data`, `forked_at`, `forked_from_version` to `class_units`; `versions`, `current_version` to `units`; creates `unit_versions` table. Content API at `/api/teacher/class-units/content` (GET resolved content + PATCH with fork-on-write). Class-local editor page at `/teacher/units/[unitId]/class/[classId]/edit`. Dashboard simplified to 3 buttons (Teach/Manage/Edit). Manage Class page restructured with tabs (Overview/Progress/Grade/Safety). All student and teacher routes use content resolution chain. Teaching Mode + Projector resolve content per selected class. **Student-Class Junction (23 Mar 2026):** Migration 041 APPLIED — `class_students` junction table decouples students from single-class ownership, `author_teacher_id` on students tracks teacher ownership, backfilled from existing data. Student session validation rewritten with separate queries (no nested joins) to avoid PGRST201 ambiguity. **Student Login Fix (23 Mar 2026):** Two stacked production bugs fixed — Vercel CDN stripping Set-Cookie from public Cache-Control responses + PostgREST PGRST201 from dual FK paths. All auth routes now set Cache-Control: private. Global header rule in next.config.ts for /api/auth/*. **Phase 0.5 Lesson Editor (24 Mar 2026):** Full drag-and-drop lesson editor with 12 components (~3,962 lines). Split-pane layout, Framer Motion Reorder, Workshop Model phases, UndoManager, auto-save, inline editing, version management. Old 545-line monolith editor replaced with thin wrapper. Content normalization handles all 4 content versions. Backfill script for stable activityId (required before production drag-to-reorder). Spec: `docs/specs/lesson-editor-phase-0.5.md`. **Student Lesson Resilience & Class Hub UX (25 Mar 2026):** Fixed student lesson page showing blank when content_data is empty — restructured page so floating buttons (AI assistant, portfolio, planning, schedule, design tools) and DesignAssistantWidget always render even when no lesson content exists. Empty state now shows helpful message with unit title instead of blank screen. Added diagnostic logging to `/api/student/unit/route.ts` to output content_data shape (version, page count, keys, null status) for debugging. Class Hub UX improvements: breadcrumb hierarchy fix (`/teacher/units/[unitId]/class/[classId]` → click class name → back to unit detail), tab renames (Safety → Badges, Metrics → New Metrics), Students tab with CRUD (add/remove students to class), inline grading in Grade tab (criterion scores directly editable per student). Framework selector on classes creation page. Hard delete for classes with confirmation dialog. Grading page exists and partially tested — `/teacher/classes/[classId]/grading/[unitId]` is 1,311 lines with criterion scoring (1-8), tags, targets, comments, moderation. Architecture finding: grades are scoped to (student_id, unit_id, class_id) and do NOT travel between classes. **Student Dashboard Multi-Class & UX Redesign (25 Mar 2026):** Student units API (`/api/student/units/route.ts`) rewritten to query `class_students` junction table for all active enrollments (+ legacy `class_id` fallback), returning units from ALL enrolled classes with class metadata (name, subject, grade_level). Student dashboard redesigned: framework-colored gradient header strips on unit cards (DESIGN=teal, SERVICE=pink, PP=purple, PYPX=amber, DIGITAL=cyan) with smart subject detection from 3 sources (`class_subject` DB field → `class_name` keywords → `unit.title` keywords). Single class name removed from dashboard header (irrelevant for multi-class students). Safety badge layout fixed — moved from side-by-side grid to vertical stacking (earned badges as horizontal scrollable strip, pending tests below as full-width cards). Open Studio tab added to Class Hub (`/teacher/units/[unitId]/class/[classId]`) using existing `OpenStudioClassView` component. **Toolkit Persistence Wiring (26 Mar 2026):** All 24 toolkit tool components wired with `useToolSession` hook for auto-save, session resume, and state tracking. Each tool gets studentId/unitId/pageId props, initializes the hook, syncs state via useEffect, and shows save status indicator. Public mode gracefully skips persistence (no studentId → no DB writes). **Discovery Engine (26 Mar 2026):** Complete 8-station interactive journey (~9,500 lines) replacing old comic-strip DiscoveryFlow. Stations: S0 Design Identity Card → S1 Campfire (binary pairs) → S2 Workshop (scenarios, people grid) → S3 Collection Wall (interests, irritations, values) → S4 Window (problem finding) → S5 Toolkit (resources, efficacy) → S6 Crossroads (AI-generated doors, fear cards) → S7 Launchpad (project statement, criteria, grand reveal). State machine (64 states, useReducer), auto-save session persistence, Kit mentor with context-aware expressions, AI reflect endpoint (9 types via Haiku), archetype scoring (6 archetypes: Maker/Researcher/Leader/Communicator/Creative/Systems Thinker), two discovery modes (Mode 1: Design, Mode 2: Service/PP/PYPx), CSS gradient backgrounds with image fallback system, student dashboard entry point (Mode 2 units only). Migration 047 for `discovery_sessions` table. Playability fixes: middleware auth protection, S7 completion guards, S6 35s auto-fallback to template doors, error state with retry/dashboard options. **Supabase Production Fix (26 Mar 2026):** Site URL changed from `http://localhost:3000` to `https://studioloom-teal.vercel.app` in Supabase dashboard (was causing CORS-like errors on teacher dashboard). Redirect URL added. Teacher layout error handling improved (try/catch/finally prevents infinite loading spinner on network errors). **Student Learning Profile (26 Mar 2026):** Comprehensive intake survey (language, design confidence, learning differences, working style) with expanded 7-system profiling based on influence factors research. Profile data wired into Open Studio AI check-ins as non-critical hints (multilingual awareness, confidence scaffolding, learning accommodations for ADHD/anxiety/dyslexia/autism). Persistent glowing banner on student dashboard for incomplete profiles (replaces auto-pop modal). ClassProfileOverview component on Class Hub Progress tab for teachers. Migration 048 for `learning_profile` JSONB on students table. **Class Gallery & Peer Review (26 Mar 2026):** Complete pin-up crit system. Teacher creates gallery rounds from Class Hub (Gallery tab) with configurable review format (comment/PMI/two-stars-wish), min reviews, anonymous mode, and deadline. Students submit work snapshots, browse peers' work one-at-a-time with sentence starters, and receive effort-gated feedback (locked until min reviews completed). 3 tables (gallery_rounds, gallery_submissions, gallery_reviews) via migration 049. 7 UI components: GalleryRoundCreator, GalleryMonitor, GalleryRoundCard, GalleryBrowser, GallerySubmitPrompt, GalleryFeedbackView, GalleryDashboardCard. Student gallery page at `/gallery/[roundId]` with 3-mode tabs (Submit → Browse & Review → Feedback). Gallery rounds shown on student dashboard with submission/review status. ~4,200 lines across 24 files. **Landing Page Rebuild (27 Mar 2026):** Complete rewrite of `/` homepage to showcase all built features. Framed AI as "extra pair of hands" not "AI that teaches" (teacher audience is AI-wary). Feature sections: Unit Builder, Design Thinking Toolkit, Teaching Mode, Design Assistant, Safety Badges, Class Gallery, Open Studio. Dark aurora hero with animated gradient. **Student Onboarding Redesign (27 Mar 2026):** "Studio Setup" — 4-screen character-creation-style first-login experience replacing old 6-page intake survey. Screen 1: Choose mentor (Kit/Sage/Spark with distinct AI personalities). Screen 2: Pick visual theme (Clean/Bold/Warm/Dark via CSS custom properties). Screen 3: Mentor-guided profile questions with animated reactions per answer. Screen 4: Welcome reveal. 3 mentors with full AI prompt modifiers injected into Design Assistant. 4 themes applied via `--st-*` CSS tokens on student layout root. Settings cog in student header for instant mentor/theme switching + "Redo full Studio Setup" option. Migration 050 (mentor_id + theme_id on students table) APPLIED. Student login hardened with 3-fallback lookup (junction → legacy class_id → author_teacher_id match). Session route resilient to pre-migration schemas. **Roadmap Consolidation (27 Mar 2026):** All roadmap-like content consolidated into single `docs/roadmap.md`. Added: Current Sprint section (tiered action plan), Intelligence Profile System (Phase 3.5), Multi-Unit-Type Architecture (Phase 1), UX Polish Items (Someday/Maybe), updated Priority Summary. `docs/priority-todo.md` replaced with pointer. CLAUDE.md "What's next" trimmed to short pointer. AI Model Settings cleaned up: removed ABCD Assessment Focus pills (too MYP-specific), removed "50+ micro sliders" note, test sandbox decoupled from AI controls page (now independent at `/admin/test-sandbox`). **Unit Generation Project Phase 0 (27 Mar 2026):** Multi-unit-type foundation complete. 4-dimension model (Unit Type × Programme × Curriculum × Standards). Migration 051 (`unit_type TEXT` + `curriculum_context TEXT` on units). `CriterionKey` extended from literal "A"|"B"|"C"|"D" to `string` — supports any criteria system (GCSE AO1-AO5, Service IPARD, etc.). Per-type criteria definitions: `SERVICE_CRITERIA` (I/P/A/R/D), `PP_CRITERIA` (A/B/C), `INQUIRY_CRITERIA` (A/B/C/D different names). Helper functions: `getCriteriaForType()`, `getCriterionKeys()`, `getCriterion()`. Full type definitions in `src/lib/ai/unit-types.ts` (509 lines). Unit type selector in wizard GoalInput (4 cards). Cascading state resets on type change. Dynamic criteria validation replaces hardcoded `["A","B","C","D"]` in all generation routes. Master spec: `docs/specs/unit-type-framework-architecture.md` (770 lines). 3-lane wizard spec: `docs/specs/wizard-lanes-spec.md` (Express/Guided/Architect). Deep audit: `docs/specs/design-hardcoding-deep-audit.md` (90 hardcoded Design instances). **Unit Generation Project Phase 1+2 (27 Mar 2026):** Guided wizard now type-aware — `GuidedConversation.tsx` rewritten with `buildTurns(unitType)` returning type-specific turn sequences (Design 7, Service 7, PP 6, Inquiry 6). New wizard fields: communityContext, sdgConnection, serviceOutcomes, partnerType (Service); personalInterest, goalType, presentationFormat (PP); centralIdea, transdisciplinaryTheme, linesOfInquiry (Inquiry). `SummaryRail.tsx` shows type-appropriate labels. `useWizardState.ts` has 10 new fields with reset on type change. New constants for Service/PP/Inquiry options. AI generation pipeline fully type-aware: `buildTeachingContext(unitType)` with 4 type-specific principle sets, `buildTypeSpecificContext(input)` for prompt injection, dynamic work time floors per type, `getActivityPhaseEnum(unitType)` + dynamic schema builders in schemas.ts, `unitType` param on all AIProvider methods. All 7 generation/suggestion routes updated. 16 files changed, +1,146/-351 lines. **Unit Generation Project Phase 3 (27 Mar 2026):** 3-lane wizard selector built. `ModeSelector.tsx` rewritten as 3-card selector (Express ⚡ / Guided 💬 / Architect 🔧). `ArchitectForm.tsx` created (~598 lines) — single scrollable form with 5 sections (Unit Identity, MYP Framework, Type-Specific Fields, Duration & Grade, Criteria Emphasis), shows/hides fields based on unitType. `WizardMode` extended with `"architect"`, `WizardPhase` extended with `"architect"`. `ConversationWizard.tsx` routes architect mode → ArchitectForm → approach selection → generation. 4 files changed, +671/-24 lines. **Phase 3 Polish (27 Mar 2026):** Lane switching mid-flow with `LaneSwitcher.tsx` pill toggle (shared `state.input` means data carries over automatically). Framework memory via `localStorage` key `studioloom_wizard_lane` — remembers last-used lane, shows "Last used" badge on ModeSelector cards. Unit type auto-detection from topic text with keyword heuristic (2+ hits required for non-design types to avoid false positives). `detectUnitTypeFromTopic()` in ConversationWizard runs before Express auto-config. Lane selector always visible on first screen (dimmed with `opacity-40 pointer-events-none` until topic entered). Unit type selector cards modernized with SVG icons replacing emoji (DesignIcon, ServiceIcon, PPIcon, InquiryIcon components). Student dashboard: Coming Up card moved above My Badges (more frequently referenced). **Generation Pipeline Parallelization (27 Mar 2026):** ~4x speedup for unit building. Criterion generation now fires all criteria in parallel via `Promise.allSettled` (was sequential `for` loop — 4 criteria × ~45s each = 3 min → now ~45s). RAG retrieval + lesson profile fetch parallelized in both `generate-outlines/route.ts` and `buildRAGCriterionPrompt()` in `prompts.ts` (was sequential, now `Promise.allSettled`). 3 files changed. **Express/Timeline Mode Parallelization (27 Mar 2026):** Timeline generation path (used by Express mode) further optimized — per-lesson concurrency increased from 2→4, RAG lookups in `buildRAGPerLessonPrompt()` and `buildRAGTimelinePrompt()` parallelized (activity cards + RAG context + lesson profiles + teacher style now fire simultaneously via `Promise.allSettled`). **Save Unit Resilience (27 Mar 2026):** Unit save now retries without `unit_type` column if migration 051 not yet applied (graceful degradation). StickyBuildBar updated to display save errors in review phase (was silently swallowing errors). **Migration 051 APPLIED (27 Mar 2026):** `unit_type TEXT` + `curriculum_context TEXT` columns now live in production. **Curriculum Context Dropdown (27 Mar 2026):** Replaced free-text curriculum context input with dropdown picker in both GoalInput (Express/Guided) and ArchitectForm (Architect lane). Type-specific preset options (Service: MYP Community Project, Service as Action, DP CAS, Duke of Edinburgh; PP: MYP Personal Project, PYP Exhibition, DP Extended Essay; Inquiry: MYP Interdisciplinary, PYP Unit of Inquiry, PBL, STEM; Design (Architect only): MYP Design, GCSE D&T, A-Level D&T, IGCSE, ACARA, PLTW) plus "Other (type your own)" custom option.

## What's done
- AI Unit Builder Wizard — 7-step conversational flow generating flexible-page units with criterion mapping, scaffolding, and 20 configurable emphasis dials
- Knowledge Base — PDF/DOCX/PPTX upload → 3-pass AI analysis → chunking → Voyage AI embedding → hybrid search (70% vector + 20% BM25 + 10% quality)
- Dual RAG retrieval — text chunks + structured LessonProfiles with quality feedback loop
- Design Assistant — Socratic mentor (Haiku 4.5, 300-token cap, Bloom's-adaptive, 3-strike effort-gating)
- Student experience — activity-first pages, 10+ response types (text, upload, voice, canvas, Decision Matrix, PMI, Pairwise Comparison, Trade-off Sliders)
- Portfolio auto-pipeline — responses + reflections auto-flow into timeline view with Behance-style export
- Quick Capture Bar — notes, photos, links during units
- Planning/Gantt view — student task tracker with time logging
- Teacher dashboard — class overview, progress tracking, grading with rubric-based criterion scores
- LMS integration — LTI 1.0a SSO (Canvas, Blackboard, Google Classroom)
- Admin AI Model Config panel — test models, 20 emphasis dials per framework, config history, dual-mode test sandbox (unit skeleton + single lesson)
- Framework-aware vocabulary — IB MYP, GCSE DT, ACARA, PLTW, A-Level DT, IGCSE DT command verbs and assessment structures
- Test Sandbox improvements — framework selector with dynamic criteria toggles, single lesson generation with full content preview (sections, scaffolding, response types, ELL tiers), improved skeleton view with summary stats
- Grade-level timing profiles — cognitive load caps by MYP year
- 3-tier ELL scaffolding — sentence starters, guided prompts, extension challenges
- Security — AES-256-GCM for BYOK keys, RLS policies, HttpOnly/Secure/SameSite cookies, .env.local gitignored
- Rate limiting — in-memory sliding window on design assistant (30/min, 200/hour per user), generic `rateLimit()` utility
- AI usage tracking — `ai_usage_log` table (migration 025), fire-and-forget logging with cost estimates per model
- Sentry error tracking — `@sentry/nextjs` with instrumentation files, global error boundary, wired into design assistant
- Vitest test suite — 38 tests across 3 files: prompt snapshots, framework vocabulary, timing profiles. `npm run test`
- Response length heuristics — warns on oversized design assistant responses (>1200 chars), flags in usage metadata
- 25 database migrations covering auth, units, knowledge base, assessment, design assistant, AI config, usage tracking
- **Report Writer free tool (Phase 6.5)** — bulk report comment generator at `/tools/report-writer`:
  - Multi-framework support (General D&T, IB MYP, GCSE DT, ACARA) with per-framework rating categories
  - Per-student skill ratings (1-5 sliders) across framework categories, with natural-language conversion to strengths/growth areas
  - Per-project/unit performance ratings (1-5) — blue-tinted columns visually distinct from gray skill columns
  - Multi-project input (up to 4 projects/units) with tag/chip UI
  - Reporting period selector (Term 1-4, Semester 1-2, Full Year) with temporal phrasing woven into AI output
  - Custom category support — teachers can add their own rating categories beyond the framework defaults
  - Tone (formal/friendly), word count (50/100/150), pronouns (he/she/they) controls
  - Bulk generation: up to 10 students per request, sequential Haiku 4.5 calls with per-student rate limiting
  - Excel/CSV upload for student data with auto-detection of columns
  - Privacy notice and email-based rate limiting (20 free uses/month)
  - Copy-to-clipboard per report, regenerate individual reports
  - Sentry error tracking, usage logging with token counts
- **Design Thinking Toolkit (Phase 6.5 — PRIMARY free tool)** — production Next.js route at `/toolkit`:
  - 42 design thinking tools across 7 categories (Ideation 10, Analysis 6, Evaluation 7, Research 6, Planning 4, Communication 4, Reflection 5)
  - **Framework-agnostic** — universal design process phases (Discover, Define, Ideate, Prototype, Test) mapping to IB MYP, GCSE DT, A-Level, ACARA, PLTW, Stanford d.school, IDEO, Double Diamond
  - Dark theme with aurora gradient hero, glassmorphism sticky filter bar, custom SVG illustrations per tool
  - Multi-factor filtering: search (with `/` keyboard shortcut + synonym matching), phase pills (color-coded), type pills, deploy mode pills
  - 4 deploy modes per tool: Present (projector), Print (worksheet), Group (collaborative), Solo (individual)
  - Card hover: 3D perspective tilt + deploy overlay slide-up
  - Difficulty badges (beginner/intermediate/advanced), time estimates, group size metadata
  - Files: `src/app/toolkit/layout.tsx`, `src/app/toolkit/page.tsx`, `src/app/toolkit/tools-data.ts`
  - Landing page integration: "Free Toolkit" nav link, dark-themed showcase section with 6 preview SVG cards + CTA button, footer link
  - Standalone HTML prototype at `docs/ideas/toolkit-v2.html` (identical 42 tools, used for rapid iteration)
  - Design spec at `docs/ideas/toolkit-design-spec.md`
- **SCAMPER Interactive Tool** — first interactive toolkit tool at `/toolkit/scamper`:
  - 3-screen flow: intro (challenge input) → working (7 SCAMPER steps) → summary (all ideas + AI insights)
  - AI-powered contextual prompts via Haiku 4.5 with adaptive difficulty (introductory → building → advanced based on idea count)
  - "Deal Me a Card" progressive prompt reveal with 10-second thinking timer (SVG circular progress ring)
  - Effort-gated Socratic feedback: client-side effort assessment → API adapts tone (low: push for specifics, medium: acknowledge + build momentum, high: celebrate + push for creative leaps)
  - Phase-aware AI feedback: nudges encourage DIVERGENT thinking (more ideas, wilder ideas, building on momentum) — never critique/evaluate during ideation
  - Micro-feedback loops: instant client-side toast on idea submission (purple glow for high effort, blue bounce for medium, amber for low), auto-dismisses after 3 seconds
  - Depth dots (1-3) on every idea card showing quality level, thinking depth meter showing average quality per step
  - Soft gating: prompts hidden until first idea written, then unlocked with slide-in animation; prompts always read-only (students must type their own ideas)
  - Input-first hierarchy: step header → textarea (primary action) → prompts (scaffolding below). Example collapsible via "See an example" toggle.
  - AI nudge returns structured JSON: `{ acknowledgment, nudge, effortLevel }` with regex fallback for malformed responses
  - Files: `src/app/toolkit/scamper/page.tsx`, `src/app/api/tools/scamper/route.ts`
- **Six Thinking Hats Interactive Tool** — at `/toolkit/six-thinking-hats`:
  - 3-screen flow: intro → working (6 hats) → summary (cross-hat insights)
  - Most sophisticated AI prompt routing: each hat has unique `hatRules` and `hatTone` that inject into system prompts
  - White=facts only, Red=emotions OK, Black=critical analysis (ONLY hat with evaluation language), Yellow=optimistic, Green=creative/divergent, Blue=process/meta
  - Hat navigation rail with per-hat completion dots and depth indicators
  - All 5 education AI patterns + gradient title animation
  - Files: `src/app/toolkit/six-thinking-hats/page.tsx`, `src/app/api/tools/six-hats/route.ts`
- **PMI Chart Interactive Tool** — at `/toolkit/pmi-chart`:
  - 3-step evaluation tool: Plus (benefits), Minus (risks), Interesting (neither good nor bad)
  - First tool with EVALUATION-phase AI tone — convergent analysis is correct here
  - "Interesting" column has special AI rules pushing students to find observations that are genuinely hard to categorize
  - Column-specific colors (green/red/purple), column navigation rail
  - All 5 education AI patterns
  - Files: `src/app/toolkit/pmi-chart/page.tsx`, `src/app/api/tools/pmi/route.ts`
- **Five Whys Interactive Tool** — at `/toolkit/five-whys`:
  - 5-step root cause analysis with causal chain visualization
  - KEY AI DIFFERENTIATOR: detects whether student goes SIDEWAYS (restating same level) vs DEEPER (finding root cause)
  - Previous answer context shown at each step with "↓ Now ask: why?" prompt
  - Chain indicators on first answer per step (primary answer feeds into next Why)
  - Purple gradient color scheme deepening with each Why level
  - Summary shows full causal chain with connectors + AI root cause analysis
  - Files: `src/app/toolkit/five-whys/page.tsx`, `src/app/api/tools/five-whys/route.ts`
- **Empathy Map Interactive Tool** — at `/toolkit/empathy-map`:
  - 4-quadrant research tool: Says (direct quotes), Thinks (private thoughts), Does (observable actions), Feels (emotions)
  - Persona field in intro screen for contextual AI prompts
  - Quadrant-specific AI rules: Says pushes for exact quotes, Thinks for private thoughts vs public statements gap, Does for camera-ready observable behaviors, Feels for CONTRADICTORY emotions
  - 2×2 grid navigation on working screen, 2×2 summary layout
  - Effort assessment includes quote detection (`hasQuote`) for the Says quadrant
  - Files: `src/app/toolkit/empathy-map/page.tsx`, `src/app/api/tools/empathy-map/route.ts`
- **Decision Matrix Interactive Tool** — at `/toolkit/decision-matrix`: Comparison Engine shape (NEW interaction pattern). Students define options + criteria, score each cell with mandatory reasoning, AI challenges reasoning quality per-criterion. ~1,100 lines frontend + ~350 lines API.
- **How Might We Interactive Tool** — at `/toolkit/how-might-we`: Guided Composition shape (NEW interaction pattern). Structured problem reframing with AI coaching. ~1,019 lines frontend + ~402 lines API.
- **Reverse Brainstorm Interactive Tool** — at `/toolkit/reverse-brainstorm`: 2-step ideation (brainstorm bad ideas → flip into solutions). ~756 lines + ~393 lines.
- **SWOT Analysis Interactive Tool** — at `/toolkit/swot-analysis`: 2×2 grid with per-quadrant AI rules. ~804 lines + ~414 lines.
- **Stakeholder Map Interactive Tool** — at `/toolkit/stakeholder-map`: 3-step discover (list → categorise → understand needs). ~839 lines + ~416 lines.
- **Lotus Diagram Interactive Tool** — at `/toolkit/lotus-diagram`: Step Sequence ideation tool. ~900 lines + ~400 lines.
- **Affinity Diagram Interactive Tool** — at `/toolkit/affinity-diagram`: Step Sequence analysis tool. ~800 lines + ~350 lines.
- **Morphological Chart Interactive Tool** — at `/toolkit/morphological-chart`: Step Sequence ideation tool. ~900 lines + ~300 lines.
- **Education AI Patterns doc** — `docs/education-ai-patterns.md`: comprehensive reference for all 5 patterns (effort-gating, Socratic feedback, staged cognitive load, micro-feedback, soft gating) plus Phase-Aware Feedback rule (ideation vs evaluation), with implementation details, code snippets, and checklist for applying to new tools
- **Interactive Toolkit Master Plan** — `docs/ideas/toolkit-interactive-tools-plan.md`: comprehensive build plan for all 42 toolkit tools. Categorizes tools into 4 interaction shapes (Step Sequence, Canvas, Comparison Engine, Guided Composition), defines detailed UX patterns for each shape, shared component architecture, AI endpoint design, build priority tiers, and timeline estimates (~40-50 days total)
- **Design Guidelines doc** — `docs/design-guidelines.md`: 42 design principles across 5 categories (AI & Pedagogy, Student Experience, Teacher Experience, Technical Patterns, Security & Privacy). Feeds the dashboard Guidelines tab. Auto-maintained on `saveme`.
- **Lesson Timing Engine (19 Mar 2026)** — Research-backed timing system enforcing the Workshop Model across all AI-generated lessons:
  - `src/lib/ai/prompts.ts` — Workshop Model mandatory in all generation prompts: 4-phase structure (Opening → Mini-Lesson → Work Time → Debrief), 1+age instruction cap formula, usable time always calculated (never raw period), extensions required on every lesson
  - `src/lib/ai/timing-validation.ts` — Server-side validation with 8 rules + auto-repair: workshop conformance, instruction cap, work time floor (45%), debrief presence, total time match, cognitive load, extensions, checkpoints. 4 timing presets (Balanced, Hands-On Heavy, Instruction Heavy, Critique Session)
  - `src/components/lesson-timing/PhaseTimelineBar.tsx` — Teacher modification UI: drag-to-resize timeline bar with colored phase blocks, lock/unlock phases, one-click presets, live rule violation warnings
  - `src/components/lesson-timing/TimingFeedbackPrompt.tsx` — Post-lesson feedback: per-phase ratings (Too Short/About Right/Too Long), actual duration logging, extension completion tracking
  - JSON schema updated: lessons now include `workshopPhases` (opening/miniLesson/workTime/debrief with durations) and `extensions` (title, description, duration, designPhase)
  - Grade profiles updated with `avgStudentAge` field; `maxHighCognitiveMinutes` aligned to 1+age rule
  - Research: `docs/lesson-timing-research-report.md` (comprehensive analysis), `docs/research/` folder with raw synthesis, data tables, integration roadmap, quick-reference card. 25+ sources from d.school, IDEO, PBLWorks, Project Zero, ASCD, cognitive science
- **Planning Tools UX Spec** — `docs/planning-tools-ux-spec.md`: 1,372-line research doc. Recommends replacing generic kanban with MYP Design Cycle phases. Keyboard-first, mobile-first, AI suggestions. 4-phase implementation roadmap.
- **Student Toolkit Access Spec** — `docs/specs/student-toolkit-access.md`: Full PRD for embedded + standalone tool modes. Two modes, one component. Auto-save, portfolio auto-capture, version history, floating launcher, AI assistant suggestions. 4 implementation phases (~9-11 days).
- **Academic Integrity System (19 Mar 2026)** — MonitoredTextarea component, integrity analysis engine, teacher integrity report:
  - `src/components/student/MonitoredTextarea.tsx` — drop-in textarea replacement silently tracking paste events, keystrokes, deletion count, focus time, tab switches, 30-second text snapshots (for playback), 10-second word count history. Zero student-facing indicators.
  - `src/lib/integrity/analyze-integrity.ts` — 6-rule scoring engine producing Human Confidence Score (0-100): paste ratio, bulk entry detection, typing speed anomaly, low editing rate, focus loss patterns, minimal active time. Returns score, level (high/medium/low), flags with severity, and summary.
  - `src/components/teacher/IntegrityReport.tsx` — teacher-facing viewer with color-coded score badge, activity metrics row (time/keystrokes/pastes/focus losses/deletion rate), severity-flagged alerts, writing playback slider (scrub through snapshots to watch text evolve), collapsible paste log.
  - `src/components/student/ResponseInput.tsx` — new `enableIntegrityMonitoring` + `onIntegrityUpdate` props. When enabled, swaps plain textarea for MonitoredTextarea. Default behavior unchanged.
  - Tests: `src/lib/integrity/__tests__/analyze-integrity.test.ts` — 12 test cases covering all 6 rules, edge cases, score clamping, helpers
- **Timing Validation Pipeline Wiring (19 Mar 2026)** — Connected timing validation to ALL generation routes + mounted UI components:
  - `src/lib/ai/schemas.ts` — added `workshopPhases` (opening/miniLesson/workTime/debrief) and `extensions` (2-3 per lesson) to page/lesson generation tool schemas. Now AI outputs structured Workshop Model timing.
  - `src/types/index.ts` — added `WorkshopPhases` and `LessonExtension` interfaces to `PageContent` so timing data flows from API → validation → UI
  - `src/app/api/admin/ai-model/test-lesson/route.ts` — calls `validateLessonTiming()` on every generated lesson. Returns auto-repaired lesson + raw AI output + `timingValidation` with issues/stats.
  - `src/app/api/teacher/generate-journey/route.ts` — validates pages with workshopPhases after `validateGeneratedPages()`, auto-repairs, returns `timingValidation` per page.
  - `src/app/api/teacher/generate-unit/route.ts` — NOW WIRED: runs `validateLessonTiming()` on each generated page after structural validation. Auto-repairs workshopPhases/extensions. Returns `timingValidation` in response.
  - `src/app/api/teacher/regenerate-page/route.ts` — NOW WIRED: same pattern — validates, repairs, returns timing report.
  - `src/components/teacher/wizard/JourneyLessonCard.tsx` — NOW WIRED: mounts interactive PhaseTimelineBar when lesson has workshopPhases data. Teachers can drag-resize phases, lock phases. Shows extensions in collapsible section. Passes `periodMinutes` and `instructionCap` props.
  - `src/app/teacher/units/[unitId]/page.tsx` — NOW WIRED: MiniPhaseBar renders compact read-only Workshop Model timing on each lesson card. Shows extension count badge. Uses `WorkshopPhases` type from shared types.
  - Tests: `src/lib/ai/__tests__/timing-validation.test.ts` — tests for auto-repair of missing phases, over-cap instruction, missing debrief, missing extensions, work time floor, presets, applyTimingPreset. Fixed `TimingContext` type to match current shape.
- **Open Studio (20 Mar 2026)** — Self-directed working mode replacing old Own Time system:
  - `supabase/migrations/029_open_studio.sql` — Drops Own Time tables (own_time_approvals, own_time_projects, own_time_sessions), creates `open_studio_status` (per-student per-unit with status/unlock/revoke lifecycle, configurable check_in_interval_min 5-30, carry_forward boolean) and `open_studio_sessions` (per working session with activity_log JSONB, drift_flags JSONB, productivity_score, ai_summary, reflection). RLS policies, indexes, updated_at trigger.
  - `src/lib/ai/open-studio-prompt.ts` — Studio critic system prompt builder (~280 lines). `buildOpenStudioSystemPrompt()` with 5 interaction modes: student_message, check_in, drift_check, documentation_nudge, alignment_check. Each mode has its own instruction builder with distinct AI personality.
  - `src/types/index.ts` — Open Studio types: `OpenStudioStatusValue`, `OpenStudioUnlockedBy`, `OpenStudioRevokedReason`, `OpenStudioProductivityScore`, `OpenStudioDriftLevel`, `OpenStudioStatus`, `OpenStudioDriftFlag`, `OpenStudioActivityEntry`, `OpenStudioSession`
  - `src/app/api/teacher/open-studio/status/route.ts` — GET (list class status), POST (grant unlock), PATCH (revoke/update settings). Supabase Auth.
  - `src/app/api/student/open-studio/status/route.ts` — GET returns unlocked status + active session. Student token auth.
  - `src/app/api/student/open-studio/session/route.ts` — POST (start session, auto-ends previous), PATCH (update focus, append activity, set reflection, end session). Lazy creation with incrementing session_number.
  - `src/app/api/student/open-studio/check-in/route.ts` — POST handles all 5 interaction types. Rate limited (10/min, 60/hour). Drift escalation with auto-revocation after 2 consecutive sessions with silent flags.
  - `src/hooks/useOpenStudio.ts` — React hook (~250 lines). Status polling (30s), check-in timer (configurable), drift detection (10 min inactivity), activity logging, session lifecycle, check-in rotation (regular → documentation nudge every 3rd → alignment check every 5th).
  - `src/components/open-studio/OpenStudioBanner.tsx` — Student-facing banner (~200 lines). Three states: start session, active session, revoked. Purple theme.
  - `src/components/open-studio/OpenStudioClassView.tsx` — Teacher dashboard section (~250 lines). Summary cards, per-student controls, check-in interval config, grant/revoke.
  - `src/components/open-studio/ReadinessIndicator.tsx` — Simple status card (~90 lines). Shows lock/unlock state with one-line message. Criteria-based readiness tracking deferred to future version.
  - `src/components/open-studio/OpenStudioUnlock.tsx` — Teacher inline unlock for progress page (~190 lines). Replaces OwnTimeUnlock.
  - `src/components/open-studio/index.ts` — Barrel exports all 4 components.
  - `src/lib/design-assistant/conversation.ts` — Modified: checks `open_studio_status` table, switches between guided and Open Studio system prompts based on DB flag.
  - Student dashboard: `OwnTimeCard` replaced with `ReadinessIndicator`
  - Student unit page: `OpenStudioBanner` mounted between hero and content, wired to `useOpenStudio` hook
  - Teacher progress page: `OwnTimeUnlock` replaced with `OpenStudioUnlock`, `OpenStudioClassView` added at bottom, API calls updated to `/api/teacher/open-studio/status`
  - Old Own Time status route returns 410 Gone with redirect message
  - Teacher dashboard: `GET /api/teacher/dashboard` now queries `open_studio_status` and returns `openStudioCount` per unit. `UnitProgressRow` shows "Studio" pill on every unit row (gray when 0 unlocked, purple with count when students active). Links to progress page for management.
  - ClassCard header split: class name/icon is a `<Link>` to class detail page, chevron is separate expand/collapse button.
- **Student Toolkit Persistence Layer (19 Mar 2026)** — Data layer for auto-save and session resumption:
  - `supabase/migrations/028_student_tool_sessions.sql` — `student_tool_sessions` table with student_id, tool_id, challenge, mode (embedded/standalone), unit/page linkage, JSONB state, versioning, status, RLS policies, updated_at trigger
  - `src/hooks/useToolSession.ts` — React hook with lazy session creation, debounced auto-save (500ms), session resumption, optimistic UI, save status tracking (idle/saving/saved/error), completion, version reset
  - `src/app/api/student/tool-sessions/route.ts` — POST (create) + GET (find/list) with student token auth
  - `src/app/api/student/tool-sessions/[id]/route.ts` — GET (retrieve) + PATCH (update state/complete) with ownership verification
- **Teaching Mode (20 Mar 2026)** — Full teaching cockpit for live classroom use:
  - `src/app/teacher/teach/[unitId]/page.tsx` — Teaching Dashboard (~710 lines). 3-column layout: lesson navigator | phase timer + live student grid | notes + extensions. Polls `/api/teacher/teach/live-status` every 8 seconds. "Needs Help" detection for students inactive 3+ min. Accepts `classId` query param for pre-selection from dashboard.
  - `src/app/teacher/teach/[unitId]/projector/page.tsx` — Dark-themed projector view (~300 lines). Phase-aware content: Opening shows hook+vocab, Mini-Lesson shows focus, Work Time shows activities+checkpoints+extensions, Debrief shows protocol/prompt. Syncs with dashboard via `postMessage`.
  - `src/app/api/teacher/teach/live-status/route.ts` — Polling endpoint (~200 lines). Per-student: online/offline, status, timeSpent, responseCount, lastActive, needsHelp flag. Summary stats.
  - `src/components/teach/PhaseTimer.tsx` — Reusable countdown timer (~390 lines). Full + compact modes. Play/pause/reset/skip, progress bars, 60-second warning pulse, phase-specific hints.
  - `src/app/teacher/units/[unitId]/page.tsx` — Added "Teach This" button (purple gradient, play icon)
  - `src/app/teacher/dashboard/page.tsx` — Added "Teach Now" quick-launch section with per-unit progress rings
- **Feature Audit (20 Mar 2026)** — `docs/studioloom-feature-audit-2026-03-20.md`: 120 features across 17 categories with testable criteria. 11 wiring findings including 47% code duplication in toolkit API routes (~2,890 wasted lines), `callHaiku()` copied 17 times. Refactoring priority matrix (P0-P3).
- **Skeleton Generation Hardening (20 Mar 2026)** — Dynamic `max_tokens` scaling based on lesson count (was hardcoded 4096, now up to 8192). Detailed error logging: stop_reason, model, usage stats, raw response on failure.
- **New Metrics / Melbourne Metrics (20 Mar 2026)** — University of Melbourne competency assessment framework. Phase 1 complete:
  - `src/lib/nm/constants.ts` — 7 competencies (starting with Agency in Learning, 12 elements), 5-level progression scales (Year 5-8 and 9-12), Student 3-point rating scale, Teacher 4-point observation scale, `NMUnitConfig` type, helper functions
  - `supabase/migrations/030_new_metrics.sql` — `competency_assessments` table (student_id, unit_id, page_id, competency, element, source, rating, comment, context JSONB), RLS policies, indexes. `nm_config` JSONB column on `units` table.
  - `supabase/migrations/032_fix_nm_page_id_type.sql` — Fixes page_id from UUID to TEXT (page IDs are nanoid strings, not UUIDs)
  - `src/components/nm/NMConfigPanel.tsx` — Teacher 3-step wizard: select competency → pick elements → assign to checkpoints (lesson pages). Pop art header. Per-checkpoint element pills. Auto-close on save.
  - `src/components/nm/CompetencyPulse.tsx` — Student self-assessment card. Pop art visual identity: hot pink header with halftone dots, yellow NM badge, 3-point rating pills (yellow/cyan/pink), "POW!" success animation. Positioned above Complete & Continue button in student lesson view.
  - `src/components/nm/ObservationSnap.tsx` — Teacher observation modal. Pop art styled. 4-point rating grid (Emerging/Developing/Applying/Extending). Comment field per element. Centered modal overlay.
  - `src/components/nm/NMResultsPanel.tsx` — Teacher results dashboard. Collapsible pop art panel. Two views: By Student (element rating pills + avg badge + teacher observation dots) and By Element (class-wide distribution bars + self/teacher averages). Mounted on unit detail page when NM enabled.
  - `src/app/api/teacher/nm-config/route.ts` — POST to save NM config per unit. Supabase SSR auth, `author_teacher_id` ownership check.
  - `src/app/api/teacher/nm-observation/route.ts` — POST teacher observation for a student. Supabase SSR auth.
  - `src/app/api/teacher/nm-results/route.ts` — GET aggregated assessment data for a unit (all students, self + teacher). Returns student names, assessments, nmConfig.
  - `src/app/api/student/nm-assessment/route.ts` — POST student self-assessment. Student token auth, rate limited (10/min).
  - `src/app/api/student/nm-checkpoint/[pageId]/route.ts` — GET checkpoint config for a page (which elements to assess). Student token auth.
  - `src/components/nm/index.ts` — Barrel exports: NMConfigPanel, CompetencyPulse, ObservationSnap, NMResultsPanel
  - Teacher dashboard: pop art "NM Results" button on each unit row when NM is enabled
  - Teaching Mode: per-student NM observation button on student cards, opens ObservationSnap modal overlay
  - Pop art visual identity: hot pink (#FF2D78), electric yellow (#FFE135), cyan (#00D4FF), black borders (#1a1a1a), halftone dot patterns, bold box shadows (6px 6px 0), Arial Black/Impact fonts, "POW!" animations
- **Unit-as-Template Architecture (20 Mar 2026)** — Units are content templates, classes are instances with per-class overrides:
  - `supabase/migrations/033_unit_template_architecture.sql` — Adds 8 columns: `classes` (academic_year, is_archived, grade_level, subject), `class_units` (nm_config JSONB, created_at, updated_at), `student_progress` (created_at), `competency_assessments` (class_id TEXT). Additive only — safe to revert code without migration rollback.
  - `src/lib/auth/verify-teacher-unit.ts` — Auth helper with 3 functions: `verifyTeacherHasUnit()`, `getNmConfigForClassUnit()` (class_units → units fallback → default), `verifyTeacherOwnsClass()`
  - All teacher NM API routes rewritten: `nm-config/route.ts` (GET/POST accept classId), `nm-observation/route.ts` (includes class_id), `nm-results/route.ts` (filters by classId)
  - All student NM API routes updated: `nm-assessment/route.ts` (looks up student class_id, reads config from class_units with fallback, includes class_id in assessment rows), `nm-checkpoint/[pageId]/route.ts` (reads NM config from class_units with fallback)
  - `src/app/api/teacher/dashboard/route.ts` — Filters archived classes (`.neq("is_archived", true)`), resolves NM enabled per class-unit (not per unit)
  - `src/app/teacher/teach/[unitId]/page.tsx` — Loads class-specific NM config when class selected, passes classId to ObservationSnap
  - `src/app/teacher/classes/[classId]/progress/[unitId]/page.tsx` — Reads NM config from class_units with fallback, passes classId to ObservationSnap
  - `src/app/teacher/units/[unitId]/page.tsx` — **Redesigned:** NM config panel + results panel REMOVED. Added "Assigned Classes" section showing clickable cards with student count, class code, NM badge, archived state. Each links to per-class settings page.
  - `src/app/teacher/units/[unitId]/class/[classId]/page.tsx` — **NEW:** Per-class settings page with breadcrumb, class info header, quick actions (View Progress, Teach This Class), NMConfigPanel + NMResultsPanel scoped to classId. Placeholder for future per-class settings (Open Studio overrides, timing).
  - NM component props updated: NMConfigPanel, NMResultsPanel, ObservationSnap all accept optional `classId` prop
  - Open Studio page: fixed 404 on "Back to Unit" link (pointed to `/unit/${unitId}` which has no index page; now points to `/unit/${unitId}/narrative`)
- **UX Polish Pass (20 Mar 2026)** — multiple student-facing and teacher-facing UX improvements:
  - Teacher unit cards: UnitThumbnail wrapped in `h-40 overflow-hidden` container, text spacing/padding fixed, badges no longer overlap
  - Student dashboard: removed motivational quotes ("Keep up the great work!"), kept just class name display
  - Open Studio JourneyMap: made sticky at top of chat page with `position: sticky; top: 0; z-index: 30`, frosted glass background, auto-compact on scroll
  - Student lesson page: sticky top nav bar with visible Dashboard button (replaces old faint white text buried in hero gradient), page counter, hamburger menu for mobile sidebar
  - Reflection redesign — "Pause & Think" with effort-gating: all reflection types (slider, checklist, text) replaced with unified written-response approach. Meaningful word count (excludes filler words like "I", "the", "is"), requires 12+ meaningful words before confidence slider appears. Sentence starters per reflection item. Effort tags (Deep thinking 20+/green, Good reflection 12+/purple, Keep going 5+/amber, Think deeper <5/red). Soft gate message below.
  - Files modified: `src/app/teacher/units/page.tsx`, `src/app/(student)/dashboard/page.tsx`, `src/app/(student)/open-studio/[unitId]/page.tsx`, `src/app/(student)/unit/[unitId]/[pageId]/page.tsx`
- **Open Studio Comic Strip Discovery (20 Mar 2026)** — DiscoveryFlow conversation rewritten as visual comic strip:
  - `src/components/open-studio/ComicPanel.tsx` (~420 lines) — complete comic panel system with:
    - `STEP_SCENES` config: per-step backgrounds, accents, emoji, header colors for all 5 discovery steps
    - 5 SVG scene illustration functions: `StrengthsScene` (mountains + treasure chest), `InterestsScene` (ocean + compass rose), `NeedsScene` (forest + telescope), `NarrowingScene` (spotlight cone), `CommitmentScene` (rocket + launchpad)
    - `MentorCharacter` SVG: purple body, golden face, explorer hat, smiling expression
    - `StudentCharacter` SVG: blue body, yellow face, brown hair, thinking expression
    - `ComicPanel` component: scene panel with SVG background, character, white speech bubble with tail, thick comic borders (3px), spring animation reveal via Framer Motion
    - `StepTransition` component: full-width cinematic panel with both characters walking, chapter header in Impact font, dotted journey path
    - `ProfileReveal` component: inline cards showing discovered strengths/interests/needs with colored borders
  - `src/components/open-studio/DiscoveryFlow.tsx` (~540 lines, heavily modified):
    - Changed from side-by-side layout (chat + profile panel) to single-column comic strip flow
    - Progress bar redesigned as mini comic-panel thumbnails with step emoji
    - Messages render as ComicPanel components with StepTransition between steps
    - ProfileReveal shown inline when stepping between discovery phases
    - Side profile panel completely removed, replaced with bottom project statement bar + archetype badge
  - `src/components/open-studio/index.ts` — barrel exports updated with ComicPanel, StepTransition, ProfileReveal, MentorCharacter, StudentCharacter
  - Design principles: Scott McCloud's "Understanding Comics" — panel closure, progressive reveal, visual storytelling through sequential art
- **Student Lesson UX Fixes (21 Mar 2026)** — Critical text input bug fixed + feedback simplification:
  - `src/hooks/usePageData.ts` — Fixed root cause of text inputs losing typed characters. `useMemo` wrapping data object to prevent new reference on every render, which was triggering useEffect resets in usePageResponses.
  - `src/hooks/usePageResponses.ts` — Belt-and-suspenders fix: `loadedPageRef` guard only loads saved responses once per page, `responsesRef` prevents save function from depending on responses state.
  - `src/components/teacher/knowledge/StudentFeedbackPulse.tsx` — Completely rewritten as pace-only feedback (🐢 Too slow, 👌 Just right, 🏃 Too fast). One tap submits. Old multi-question emoji-per-question UI removed.
  - `src/app/(student)/unit/[unitId]/[pageId]/page.tsx` — Removed inline reflection section (~80 lines). Added auto-save indicator. Simplified feedback pulse modal.
  - `src/app/api/student/pace-feedback/route.ts` — NEW student pace feedback endpoint. Uses `requireStudentAuth` (cookie token session). Stores in `lesson_feedback` with `teacher_id=NULL`, `lesson_profile_id=NULL`, `feedback_data.pace` for timing model.
  - `supabase/migrations/036_student_pace_feedback.sql` — Makes `lesson_profile_id` and `teacher_id` nullable on `lesson_feedback`. Adds `lesson_feedback_page_idx` index. **APPLIED 21 Mar 2026.**
  - `src/app/api/teacher/pace-feedback/route.ts` — NEW teacher endpoint returning aggregated pace distribution per page per unit.
  - `src/components/teacher/PaceFeedbackSummary.tsx` — NEW stacked bar visualization (red=too_fast, green=just_right, amber=too_slow) mounted on class progress page.
- **School Calendar / Term System (21 Mar 2026)** — Per-teacher academic calendar for chronological unit-class tracking:
  - `supabase/migrations/037_school_calendar.sql` — `school_calendar_terms` table (teacher_id, academic_year, term_name, term_order, start_date, end_date). Adds `term_id` FK on `class_units`. RLS policies, indexes. **NOT YET APPLIED.**
  - `src/app/api/teacher/school-calendar/route.ts` — NEW GET/POST calendar CRUD. Upserts calendar terms per academic year.
  - `src/app/api/teacher/class-units/route.ts` — NEW PATCH endpoint to update `term_id` on class_units junction.
  - `src/components/teacher/SchoolCalendarSetup.tsx` — NEW calendar setup UI. Quick templates (4 Terms, 2 Semesters, 3 Trimesters, Custom). Mounted in teacher settings page.
  - `src/app/teacher/units/[unitId]/class/[classId]/page.tsx` — Added term picker dropdown (fetches terms, auto-saves via PATCH). Schedule info display (lesson count + next class when term selected).
  - `src/app/teacher/settings/page.tsx` — Mounted SchoolCalendarSetup in School & Teaching tab.
  - Modeled after ManageBac/Toddle approach but simplified for individual teachers (not school-admin level).
- **Timetable & Scheduling System (21 Mar 2026)** — Rotating cycle timetable with full scheduling engine:
  - `src/lib/scheduling/cycle-engine.ts` (~427 lines) — Core cycle day calculation engine. Anchor-based modular arithmetic for 5/6/8/10-day rotating cycles. Supports excluded dates (holidays), per-term reset, weekday-only advancement. Functions: `getCycleDay()`, `getNextClassDate()`, `getLessonCountInRange()`, `getScheduleForRange()`.
  - `supabase/migrations/038_timetable.sql` — `school_timetable` table (teacher_id, cycle_length, anchor_date, anchor_cycle_day, reset_each_term, excluded_dates JSONB, ical_url) + `class_meetings` table (timetable_id, class_id, cycle_day, period_number, room). RLS policies, indexes. **APPLIED 21 Mar 2026.**
  - `src/app/api/teacher/timetable/route.ts` (~266 lines) — Timetable CRUD (GET/POST/PUT). Handles excluded_dates and ical_url.
  - `src/app/api/teacher/timetable/import-ical/route.ts` (~100 lines) — POST accepts `{ ical_url }` or `{ ical_content }` (file upload). Fetches URL with 10s timeout, parses with ical-parser, matches events to teacher's classes.
  - `src/lib/scheduling/ical-parser.ts` (~200 lines) — Lightweight RFC 5545 iCal parser. Extracts VEVENT entries, detects holidays (all-day events + keyword matching: holiday, break, no school, PD day, etc.), expands multi-day holidays, line unfolding per spec. Functions: `parseICal()`, `extractMeetings()`.
  - `src/app/api/teacher/schedule/lessons/route.ts` (~161 lines) — Computed schedule endpoint with 3 modes: `next` (next class date), `count` (lesson count in date range), `calendar` (full schedule for range).
  - `src/app/api/student/next-class/route.ts` (~153 lines) — Student-facing API returning next scheduled class. Rate limited (30/min). Student token auth.
  - `src/app/teacher/settings/page.tsx` — **Restructured:** new "Workshop & Equipment" tab (wrench icon) split from School tab. Rotating Cycle config UI (cycle length 5-10, anchor date, anchor cycle day, reset-each-term toggle). Class Meetings manager (add/remove per cycle day/period/room). Non-School Days (excluded dates) with date picker + optional label + amber pills. iCal Import section (URL input + .ics file upload).
  - `src/app/(student)/dashboard/page.tsx` — Student Continue card now shows "Next: Day X, PY — DayName" blue pill with room info via `/api/student/next-class`.
  - Fixed: `loadClasses()` teacher filter (added `.eq("author_teacher_id", user.id)` — was missing, relied on RLS only).
  - Fixed: `rateLimit()` call signature in student next-class route (was factory pattern, correct is `(key, windows[])`).
  - Fixed: escaped bracket directory `\[badgeId\]` → `[badgeId]` from parallel session.
- **Lesson Scheduling & Overrides (22 Mar 2026)** — Lesson-to-date mapping with teacher override controls:
  - `src/components/teacher/LessonSchedule.tsx` (~400 lines) — Maps unit lessons to real calendar dates. Shows session number, lesson title, date, cycle day badge (color-coded). "Continues next class" toggle adds extra sessions (max 4 per lesson). Per-lesson notes. Today highlight, past lesson graying, overflow warning when lessons exceed term. Calls `/api/teacher/schedule/lessons?mode=next` for date computation.
  - `src/components/teacher/TimetableGrid.tsx` (~300 lines) — Visual Day×Period grid replacing row-by-row class meeting entry. Click empty cell → dropdown to pick class + room → Add. Click filled cell → removes. Color-coded by class (8-color palette). Quick-add row below grid for keyboard-first entry. Handles meetings with and without period numbers.
  - `src/app/api/teacher/class-units/route.ts` — Extended: GET endpoint for loading class-unit data including schedule_overrides. PATCH now accepts `schedule_overrides` JSONB.
  - `src/app/teacher/units/[unitId]/class/[classId]/page.tsx` — Mounted LessonSchedule between Schedule Info and NM Config sections. Loads schedule_overrides from class_units, saves via PATCH.
  - `src/app/teacher/settings/page.tsx` — Replaced manual class meetings section with TimetableGrid component.
  - `src/components/teacher/SchoolCalendarSetup.tsx` — Fixed: Quick Setup template buttons now always visible (were hidden after save). Label changes dynamically: "Quick Setup" → "Change Structure".
  - `supabase/migrations/037_school_calendar.sql` — Added `schedule_overrides JSONB DEFAULT '{}'` column to `class_units` table.
  - Schedule overrides JSONB shape: `{ extra_sessions: { pageId: N }, skip_dates: ["YYYY-MM-DD"], notes: { pageId: "text" } }`
- **Safety Badge System (21 Mar 2026)** — Complete workshop safety certification for unit access gating:
  - `supabase/migrations/035_safety_badges.sql` — 5 tables: `badges` (teacher-created badge definitions with question pools + learning cards), `student_badges` (earned/granted badges with score, attempt, expiry), `unit_badge_requirements` (badge-to-unit prerequisites), `safety_sessions` (test session tracking), `safety_results` (per-attempt scoring). RLS policies with `::text` casts, indexes, updated_at triggers. **APPLIED 21 Mar 2026.**
  - `src/lib/safety/types.ts` — BadgeQuestion (5 types: multiple_choice, true_false, scenario, sequence, match), LearnCard, BadgeDefinition, BadgeResult, StudentBadge types
  - `src/lib/safety/badge-thumbnails.ts` — Deterministic gradient thumbnails per badge
  - `src/app/teacher/safety/page.tsx` — Teacher badge management listing page
  - `src/app/teacher/safety/create/page.tsx` — Create badge form with learning cards + question pool builder
  - `src/app/teacher/safety/[badgeId]/page.tsx` — Badge detail with 4 tabs (Overview, Questions, Learn Cards, Results). Assign modal with "Require for Unit" and "Grant Badge Directly" flows. Results tab with class filter, student names, pass/fail/granted status, summary stats.
  - `src/app/(student)/safety/page.tsx` — Student badge listing
  - `src/app/(student)/safety/[badgeId]/page.tsx` — 3-screen test flow: learn cards → quiz (randomized questions) → results with score
  - `src/app/api/teacher/badges/route.ts` — CRUD for badges
  - `src/app/api/teacher/badges/[id]/route.ts` — Single badge GET/PATCH/DELETE
  - `src/app/api/teacher/badges/[id]/assign/route.ts` — POST to assign badge to unit or grant to students
  - `src/app/api/teacher/badges/[id]/results/route.ts` — GET results with student names joined, class filter, failed attempts included, summary stats computed
  - `src/app/api/teacher/badges/class-status/route.ts` — Per-student badge pass/fail status for teacher progress page
  - `src/app/api/teacher/badges/classes-students/route.ts` — Returns classes with student lists for assign modal
  - `src/app/api/student/safety/pending/route.ts` — Returns badges required by student's units that aren't yet earned
  - `src/app/api/student/safety/badges/[badgeId]/route.ts` — GET badge for student test-taking
  - `src/app/api/student/safety/badges/[badgeId]/submit/route.ts` — POST auto-scores test, awards badge if passed
  - `src/app/api/student/safety/check-requirements/route.ts` — Checks if student has all required badges for a unit
  - `src/components/student/BadgeGate.tsx` — Client-side unit access gate component
  - `src/components/teacher/SafetyRequirements.tsx` — Teacher unit management component
  - `src/app/tools/safety/page.tsx` — Free public safety quiz tool (dark theme)
  - Student dashboard: amber "Required Safety Tests" banner with per-badge cards, "Take Test" CTA linking to test flow. Compact earned badges display (green pills) when no pending tests.
  - Teacher dashboard: amber shield chip on unit rows showing required badge count, linking to progress page
  - Teacher progress page: "Safety" column showing per-student badge completion (✓ green/✗ red/— gray/fraction amber), class-wide completion percentage
  - Unit access gating: server-side gate on narrative page + client-side gate on lesson pages, both show amber "Safety Tests Required" screen with links to take tests
  - Badge detail page Results tab: class filter dropdown, student names, status labels (Passed/Granted/Failed/Expired), teacher notes, failed attempts included
- **Unit Forking Architecture (22 Mar 2026)** — Copy-on-write per-class content editing:
  - `supabase/migrations/040_unit_forking.sql` — Adds `content_data JSONB DEFAULT NULL`, `forked_at TIMESTAMPTZ`, `forked_from_version INTEGER` to `class_units`. Adds `versions JSONB DEFAULT '[]'`, `current_version INTEGER DEFAULT 1` to `units`. Creates `unit_versions` table (full content snapshots with version_number, label, source_class_id). RLS policies for teacher read/insert on unit_versions.
  - `src/lib/units/resolve-content.ts` (~200 lines) — `resolveClassUnitContent(masterContent, classContent)` pure function (classContent ?? masterContent). `getResolvedContent(supabase, unitId, classId)` server-side with DB lookups. `ensureForked(supabase, unitId, classId)` copy-on-write (deep-copies master on first edit). `saveAsVersion(supabase, unitId, classId, label)` for P1 version history.
  - `src/app/api/teacher/class-units/content/route.ts` — GET (returns resolved content with fork metadata) + PATCH (fork-on-write: calls ensureForked, then updates class_units.content_data). Uses `requireTeacherAuth` + `createAdminClient`. `.maybeSingle()` for graceful handling when no fork exists.
  - `src/app/teacher/units/[unitId]/class/[classId]/edit/page.tsx` — Class-local content editor. Fork indicator badge (amber for forked, blue for master). Title/type/criterion editing per page. Add/remove/reorder pages. Saves via PATCH to content API (fork-on-write).
  - Student API routes updated: `src/app/api/student/unit/route.ts` and `src/app/api/student/units/route.ts` both import `resolveClassUnitContent` and apply resolution chain to returned content.
  - Teacher routes updated: class-unit settings page, Teaching Mode, Projector all resolve content per class. Teaching Mode stores class-unit content map in ref, passes classId to projector URL.
  - Dashboard simplified: 5 buttons → 3 (Teach purple, Manage class-colored, Edit Unit outline). "Manage" links to tabbed class-unit page. "Edit Unit" links to class-local editor.
  - Manage Class page restructured with tab bar: Overview (existing settings), Progress (link to progress view), Grade (link to grading), Safety (badge management). Quick actions: Teach + Edit Unit.
  - Key invariant: page IDs preserved during forking — progress, NM checkpoints, schedule overrides all reference page IDs.
- **Unit Forking P1 — Version History & Controls (23 Mar 2026):**
  - `src/app/api/teacher/units/versions/route.ts` — NEW: GET returns version metadata (with source class names resolved) + active fork list. POST saves class fork as new master version via `saveAsVersion()`.
  - `src/app/api/teacher/class-units/content/route.ts` — Extended with POST handler for reset-to-master (sets content_data/forked_at/forked_from_version to NULL).
  - `src/app/teacher/units/[unitId]/class/[classId]/edit/page.tsx` — Added "Save as Version" button (opens modal with label input, posts to versions API, shows green toast with version number). Added "Reset to Master" button (confirmation dialog with warning + tip to save version first, clears fork).
  - `src/app/teacher/units/[unitId]/page.tsx` — Added collapsible "Versions" section: shows original v1, saved versions (label, source class, date), and active class forks with fork icon. Fork indicator ("Customized") shown on assigned class cards.
  - `src/app/teacher/dashboard/page.tsx` — Amber "Customized" badge with fork SVG icon on dashboard unit rows when `isForked` is true.
  - `src/app/api/teacher/dashboard/route.ts` — Emits `isForked: !!(cu).content_data` per unit.
  - `src/types/dashboard.ts` — Added `isForked?: boolean` to `DashboardUnit`.
- **Phase 0.5 Lesson Editor (24 Mar 2026):** Full drag-and-drop lesson editor replacing old 545-line basic editor. 12 components (~3,962 lines total) at `src/components/teacher/lesson-editor/`. Split-pane layout: LessonSidebar (drag-to-reorder lessons) + editor area (Workshop Model phases, drag-to-reorder activities, inline editing). Framer Motion Reorder for drag-and-drop. UndoManager (30-snapshot depth, `structuredClone`). Auto-save via useAutoSave (800ms debounce, PATCHes to content API with fork-on-write). Keyboard shortcuts (Cmd+Z/Shift+Z). Version management (Save as Version modal, Reset to Master with confirmation). 6 activity type templates in ActivityBlockAdd (discussion, hands-on, research, reflection, presentation, assessment). Content normalization via `normalizeContentData()` handles all 4 content versions (v1 object pages, v2 array pages, v3 journey, v4 timeline). Class-local editor page rewritten as thin wrapper rendering LessonEditor component.
  - `src/components/teacher/lesson-editor/LessonEditor.tsx` — Main orchestrator (~870 lines). Split-pane, sticky timing bar, Reorder.Group for activities, keyboard shortcuts, version management.
  - `src/components/teacher/lesson-editor/useLessonEditor.ts` — Main state hook. Loads content via GET `/api/teacher/class-units/content`, normalizes with `normalizeContentData()`, manages pages/selection/undo/redo/auto-save.
  - `src/components/teacher/lesson-editor/useAutoSave.ts` — Debounced auto-save (800ms). PATCHes to content API. Compares JSON to skip no-op saves.
  - `src/components/teacher/lesson-editor/UndoManager.ts` — Pure undo/redo stack (30 max, structuredClone).
  - `src/components/teacher/lesson-editor/LessonSidebar.tsx` — Left sidebar with Reorder.Group for lesson reordering.
  - `src/components/teacher/lesson-editor/ActivityBlock.tsx` — Full activity card with drag handle, inline editing, response type selector.
  - `src/components/teacher/lesson-editor/ActivityBlockAdd.tsx` — 6 activity type templates (discussion, hands-on, research, reflection, presentation, assessment).
  - `src/components/teacher/lesson-editor/PhaseSection.tsx` — Collapsible Workshop Model phase wrapper (Opening/Mini-Lesson/Work Time/Debrief).
  - `src/components/teacher/lesson-editor/LessonHeader.tsx` — Page title + learning goal inline editable.
  - `src/components/teacher/lesson-editor/ExtensionBlock.tsx` — Extension activity card.
  - `src/components/teacher/lesson-editor/InlineEdit.tsx` — Click-to-edit component.
  - `src/components/teacher/lesson-editor/index.ts` — Barrel exports.
  - `src/app/teacher/units/[unitId]/class/[classId]/edit/page.tsx` — Rewritten from 545 lines to ~30 lines (thin wrapper rendering LessonEditor).
  - `src/types/index.ts` — Added `"canvas"` to ResponseType union.
  - `scripts/backfill-activity-ids.ts` + `scripts/backfill-activity-ids.mjs` — Backfill stable activityId on all existing sections (required before drag-to-reorder). Migrates student_progress response keys from `section_N` to `activity_<id>`. Plain .mjs version avoids tsx dependency issues.
  - Spec: `docs/specs/lesson-editor-phase-0.5.md`. Usage guide: `src/components/teacher/lesson-editor/USAGE-GUIDE.md`.
- **Phase 0.5 Lesson Editor Enhancements (24 Mar 2026):** Cross-container drag-and-drop, AI text field assist, sidebar improvements:
  - `src/components/teacher/lesson-editor/DndContext.tsx` — NEW: React context for cross-container drag state. Tracks `isDragging`, `payload` (activity + label + icon + source), `activeDropZone`. Exports `DndProvider`, `useDndContext`, `DragPayload`.
  - `src/components/teacher/lesson-editor/DropZone.tsx` — NEW: Drop target component appearing inside each Workshop Model phase when drag is active. HTML5 DnD events (onDragOver/onDragLeave/onDrop). Phase-colored glow on hover (indigo/blue/emerald/amber). AnimatePresence visibility toggle.
  - `src/components/teacher/lesson-editor/AITextField.tsx` — NEW: Textarea with purple # button (AI brand mark) for content suggestions. Calls `/api/teacher/lesson-editor/ai-field`. Shows 3 numbered suggestions in dropdown; click to apply. Context-aware: field type (hook/focus/protocol/prompt), lesson title, learning goal, phase.
  - `src/app/api/teacher/lesson-editor/ai-field/route.ts` — NEW: POST endpoint using Haiku 4.5 for field-specific suggestions. `FIELD_PROMPTS` map with 4 field types (hook: 6 hook types, focus: mini-lesson descriptions, protocol: 9 debrief protocols, prompt: reflective questions). Returns `{ suggestions: string[] }`.
  - `src/components/teacher/lesson-editor/BlockPalette.tsx` — Modified: PaletteBlock now uses HTML5 draggable with drag handle (`⠿`). Removed internal AnimatePresence wrapper (LessonEditor controls visibility). `useDndContext` integration for drag state signaling.
  - `src/components/teacher/lesson-editor/LessonSidebar.tsx` — Modified: Added "+ New" button in sidebar header (always visible). Flex row layout: "Lessons" label + "New" button.
  - `src/components/teacher/lesson-editor/LessonEditor.tsx` — Modified: Wrapped with `<DndProvider>`. DropZone added to all 4 Workshop Model phases. 4 textareas replaced with AITextField (hook, focus, protocol, prompt).
  - `src/components/teacher/lesson-editor/index.ts` — Added exports: DropZone, AITextField, DndProvider, useDndContext.

- **Student Dashboard Multi-Class Enrollment Fix (25 Mar 2026):** Student units API rewritten for multi-class support:
  - `src/app/api/student/units/route.ts` — Queries `class_students` junction table for all active enrollments + legacy `students.class_id` fallback. Collects class IDs into Set, uses `.in("class_id", classIdArray)`. Returns class metadata (name, subject, grade_level) per unit. Uses `resolveClassUnitContent()` for content resolution.
  - `src/app/(student)/dashboard/page.tsx` — Major redesign:
    - Extended `UnitWithProgress` interface with `class_id`, `class_name`, `class_subject`, `class_grade_level`
    - Added `detectSubject()` smart subject detection function: checks `class_subject` (DB) → `class_name` → `unit.title` with keyword matching against SUBJECT_MAP (DESIGN=teal, SERVICE=pink, PP=purple, PYPX=amber, DIGITAL=cyan, plus general subjects)
    - Framework-colored gradient header strips on unit cards with bold subject label + progress ring overlay
    - 2-column grid layout (`grid-cols-1 sm:grid-cols-2`)
    - Single class name removed from header (irrelevant for multi-class students)
    - Safety badge layout restructured: earned badges as compact horizontal scrollable strip above, pending tests below as full-width container with internal 2-col grid. Removed `lg:sticky` that caused scroll overlap.
  - `src/app/teacher/units/[unitId]/class/[classId]/page.tsx` — Added Open Studio tab to Class Hub (7th tab) using existing `OpenStudioClassView` component with info card about check-ins, drift detection, auto-revocation.
- **Toolkit Persistence Wiring (26 Mar 2026):** All 24 toolkit tool components wired with `useToolSession` hook:
  - Each tool gets `studentId`, `unitId`, `pageId` props for persistence context
  - Hook initialized with `mode === 'public' ? 'standalone' : mode` (public mode gracefully skips DB writes)
  - State sync via `useEffect` → `updateToolSession(state)` on relevant state changes
  - Fixed-position save status indicator (⟳ Saving... / ✓ Saved / ✕ Save failed) in top-right corner
  - Tools wired: AffinityDiagramTool, BiomimicryTool, BrainstormWebTool, DecisionMatrixTool (stub), DesignSpecificationTool, EmpathyMapTool, FeedbackCaptureGridTool, FishboneTool, FiveWhysTool, HowMightWeTool (stub), ImpactEffortMatrixTool, JourneyMapTool, LotusDiagramTool, MindMapTool, MorphologicalChartTool, PairwiseComparisonTool, PmiChartTool, PointOfViewTool, ReverseBrainstormTool, ScamperTool (reference impl), SixHatsTool, StakeholderMapTool, SwotAnalysisTool, SystemsMapTool, UserPersonaTool
  - Key pattern: `useToolSession` type constraint only accepts `'embedded' | 'standalone'` — must use ternary for `'public'` mode
- **Discovery Engine (26 Mar 2026):** Complete 8-station interactive journey (~9,500 lines):
  - `src/app/(student)/discovery/[unitId]/page.tsx` (26 lines) — thin wrapper rendering DiscoveryShell
  - `src/app/(student)/discovery/[unitId]/layout.tsx` (15 lines) — passthrough layout (no nav header)
  - `src/app/api/discovery/session/route.ts` (153 lines) — session CRUD (GET/POST/PATCH), uses requireStudentAuth
  - `src/app/api/discovery/reflect/route.ts` (368 lines) — shared Haiku AI endpoint, 9 types: s2_panic, s3_irritation, s3_reveal, s4_problem, s5_reveal, s6_doors, s6_fear, s7_criteria, s7_grand_reveal
  - `src/components/discovery/DiscoveryShell.tsx` (188 lines) — main orchestrator: session lifecycle, station rendering, Kit mentor, progress bar, navigation, exit button, error state
  - `src/components/discovery/stations/Station0Identity.tsx` through `Station7Launchpad.tsx` (~3,759 lines total) — all 8 station components
  - `src/components/discovery/stations/StationRenderer.tsx` — delegates to correct station based on state machine
  - `src/components/discovery/stations/TransitionScreen.tsx` — between-station transition animations
  - `src/components/discovery/KitMentor.tsx` — Kit character with context-aware expressions
  - `src/components/discovery/ProgressBar.tsx` — 8-station progress indicator
  - `src/components/discovery/StationBackground.tsx` — CSS gradients with image fallback
  - `src/components/discovery/DiscoveryImage.tsx` — image loader with `checkImageExists()` probe
  - `src/hooks/useDiscoverySession.ts` (460 lines) — state machine driver, auto-save, resume support
  - `src/lib/discovery/state-machine.ts` (354 lines) — useReducer, 64 states, guards, STATION_META
  - `src/lib/discovery/types.ts` (509 lines) — DiscoveryProfile, station data types, 6 archetypes
  - `src/lib/discovery/scoring.ts` (312 lines) — weighted archetype scoring with normalization
  - `src/lib/discovery/content/` (8 station content files, ~1,631 lines) — all content pools
  - `src/lib/discovery/assets.ts` (123 lines) — image path registry with fallback
  - `src/lib/discovery/kit-expressions.ts` (148 lines) — Kit personality per state
  - `public/discovery/` — 24 images (8 backgrounds, 4 Kit expressions, 12 tool icons)
  - `supabase/migrations/047_discovery_sessions.sql` — APPLIED
  - Student dashboard: "Start Discovery Journey" link on Mode 2 unit cards only (SERVICE/PP/PYPX)
  - Middleware: `/discovery` added to student auth protection
  - S7 guards: `station_7_grand_reveal` requires `archetypeResult !== null`
  - S6: 35-second hard auto-fallback to template doors if AI generation hangs
  - Error state: "Try Again / Back to Dashboard" when session init fails
- **Teacher Layout Error Handling (26 Mar 2026):** `src/app/teacher/layout.tsx` — wrapped `loadTeacher()` in try/catch/finally so CORS or network errors don't leave page stuck on loading spinner. Errors logged to console. `setLoading(false)` guaranteed in finally block.
- **Student Learning Profile (26 Mar 2026):** Comprehensive intake survey with expanded 7-system profiling based on influence factors research:
  - `supabase/migrations/048_learning_profile.sql` — Adds `learning_profile JSONB` to students table. **APPLIED 26 Mar 2026.**
  - Intake survey modal on student dashboard: language (primary + additional languages at home), design confidence (1-5 with emoji scale), learning differences (ADHD, dyslexia, anxiety, autism, physical, other + free text), working style (solo/pair/group/flexible), sensory preferences, and support needs
  - Profile data wired into Open Studio AI check-ins (`src/app/api/student/open-studio/check-in/route.ts`): multilingual awareness hints, design confidence scaffolding, learning accommodation adjustments for ADHD (shorter check-ins), anxiety (gentler tone), dyslexia (simpler language), autism (more structured). All wrapped in try/catch (non-critical).
  - `src/components/teacher/ClassProfileOverview.tsx` — Teacher view of class-wide profile summary on Class Hub Progress tab. Shows language distribution, confidence spread, learning differences aggregation.
  - Persistent glowing banner on student dashboard when profile incomplete (replaces auto-pop modal that students could dismiss and never see again). Purple gradient, `animate-ping` notification dot, "Takes 30 seconds" encouragement.
- **Class Gallery & Peer Review (26 Mar 2026):** Complete digital pin-up crit system (~4,200 lines across 24 files):
  - `supabase/migrations/049_class_gallery.sql` — 3 tables: `gallery_rounds` (teacher-created critique sessions with review_format, min_reviews, anonymous, deadline), `gallery_submissions` (student work snapshots with content JSONB + context notes), `gallery_reviews` (peer feedback with unique constraint on submission+reviewer). RLS policies, indexes, cascade deletes, updated_at trigger. **APPLIED 26 Mar 2026.**
  - Teacher API routes: `src/app/api/teacher/gallery/route.ts` (GET list rounds with submission counts, POST create round), `src/app/api/teacher/gallery/[roundId]/route.ts` (GET monitoring view with submissions + student names + review counts, PATCH update/close round, DELETE with cascade)
  - Student API routes: `src/app/api/student/gallery/rounds/route.ts` (GET open rounds for enrolled classes via class_students junction + legacy fallback), `src/app/api/student/gallery/submit/route.ts` (POST work snapshot, rate limited 5/min), `src/app/api/student/gallery/submissions/route.ts` (GET browse submissions excluding own, randomized, anonymous handling), `src/app/api/student/gallery/review/route.ts` (POST peer review, prevents self-review + duplicates, rate limited), `src/app/api/student/gallery/feedback/route.ts` (GET effort-gated — returns `{ locked: true }` if < min_reviews completed)
  - 7 UI components at `src/components/gallery/`: GalleryRoundCreator (modal with title, pages, format, min reviews, anonymous, deadline), GalleryMonitor (teacher dashboard: submissions, review progress, per-student table, close round), GalleryRoundCard (compact card for Class Hub listing), GalleryBrowser (~556 lines — one-at-a-time submission view with 3 review formats + sentence starters per format + progress tracking), GallerySubmitPrompt (banner with context note), GalleryFeedbackView (~261 lines — locked state with blurred preview + unlock count, unlocked shows all peer reviews), GalleryDashboardCard (~207 lines — student dashboard card with status badges)
  - `src/app/(student)/gallery/[roundId]/page.tsx` — Student gallery page with 3-mode tabs: Submit → Browse & Review → Feedback. Auto-detects initial mode based on submission/review state. Effort-gating: can't access Feedback tab until min reviews completed.
  - Gallery tab added to Class Hub (`/teacher/units/[unitId]/class/[classId]`) with round listing, create/monitor modals
  - Gallery rounds section on student dashboard between safety tests and Due This Week
  - `/gallery` added to middleware student auth protection
  - 3 review formats: comment (with sentence starters), PMI (plus/minus/interesting), Two Stars & a Wish
  - `formatDate()` and `getDaysUntil()` utility functions added to `src/lib/utils.ts`
  - Gallery types added to `src/types/index.ts`: GalleryStatus, ReviewFormat, GalleryRound, GallerySubmission, GalleryReview, GalleryRoundWithStats
  - **No lucide-react dependency** — all icons use inline SVGs (lucide-react not installed in project)

## What's next

**The canonical roadmap is at `docs/roadmap.md`.** It contains ALL phases, priorities, specs, and the current sprint plan. Everything is consolidated there — do not add roadmap items to this file.

**Current focus (March 2026):**
1. **Unit Generation Project** — Phases 0+1+2+3 COMPLETE 27 Mar including Phase 3 polish (lane switching, framework memory, auto-detection, SVG icons, generation parallelization ~4x speedup). Specs: `docs/specs/wizard-lanes-spec.md`, `docs/specs/unit-type-framework-architecture.md`.
2. Push pending commits (blocked by school proxy)
3. Wire MonitoredTextarea into submissions + run e2e testing sprint
4. Phase 0.5 remaining (debrief library, prompt enrichment, student preview)
5. API deduplication (~2,890 wasted lines from 17× copied `callHaiku()`)
6. Then: Lesson Plan Converter, Year Planner, Intelligence Profile System

**E2E test checklists** (all in CLAUDE.md or linked docs): content forking (7 points), NM (`docs/nm-test-checklist.md`), Open Studio (`docs/open-studio-test-checklist.md`), safety badges, pace feedback, timetable/scheduling, grading page.

**Architecture gaps to resolve:** student_progress table lacks class_id (ambiguous for multi-class enrollment), skeleton generation intermittent "missing lessons array" error.

## Known issues / blockers
- **Cleanup needed (25 Mar 2026):** (1) Remove temp debug panel from student lesson page (added for content_data diagnosis — yellow diagnostic box). (2) Remove diagnostic `console.log` from `src/app/api/student/unit/route.ts` (outputs content_data shape). (3) Set `subject` field on classes in Supabase for best card label results (currently relies on name-based detection).
- **13+ commits pending push (26 Mar 2026)** — School network proxy blocks git push (HTTP 403 after CONNECT). Includes toolkit wiring, Discovery Engine (~9,500 lines), playability fixes, teacher layout error handling. Push when on non-proxied network. Vercel auto-deploy will activate once pushed. **Supabase Site URL already updated to `https://studioloom-teal.vercel.app` (26 Mar 2026).**
- **student_progress table lacks class_id field (25 Mar 2026 — ARCHITECTURE GAP)** — When a student is enrolled in multiple classes and takes the same unit in both, progress records are ambiguous. The `student_progress` table has (student_id, unit_id, page_id) as PK, but no class_id. This breaks the multi-class enrollment model introduced in migration 041. Grades use (student_id, unit_id, class_id), so this is a schema inconsistency. Design decision needed: (a) add class_id to student_progress PK (breaking change, requires migration + data backfill), (b) accept ambiguity for now (student can only be in one class per unit), or (c) move progress tracking to class_units level. This only matters if the same student enrolls in two classes teaching the same unit simultaneously — uncommon but architecturally unsound.
- **Migration 025 (usage tracking) ✅ CONFIRMED APPLIED 24 Mar 2026** — `ai_usage_log` table exists. Usage tracking is live. **Migration 028 (student tool sessions) — status unknown, needs check.** Migration 029 (Open Studio) APPLIED 20 Mar 2026. Migration 030 (NM) APPLIED 20 Mar 2026. **Migration 032 (NM page_id UUID→TEXT fix) ✅ APPLIED 21 Mar 2026.** **Migration 033 (unit-as-template architecture) ✅ APPLIED 21 Mar 2026.** **Migration 036 (student pace feedback — nullable columns) ✅ APPLIED 21 Mar 2026.** **Migration 037 (school calendar + term_id + schedule_overrides on class_units) ✅ CONFIRMED APPLIED 24 Mar 2026** — `school_calendar_terms` table + `class_units.term_id` + `class_units.schedule_overrides` all exist. All scheduling UI unblocked. **Migration 035 (safety_badges) ✅ APPLIED 21 Mar 2026.** **Migration 038 (timetable + class_meetings) ✅ APPLIED 21 Mar 2026.** **Migration 040 (unit forking) ✅ APPLIED 22 Mar 2026.** **Migration 041 (student-class junction) ✅ APPLIED (discovered 23 Mar 2026).** **Migration 047 (discovery_sessions) ✅ APPLIED 26 Mar 2026.** **Migration 048 (learning_profile JSONB on students) ✅ APPLIED 26 Mar 2026.** **Migration 049 (class_gallery — gallery_rounds, gallery_submissions, gallery_reviews) ✅ APPLIED 26 Mar 2026.** **Migration 050 (studio_preferences — mentor_id + theme_id on students) ✅ APPLIED 27 Mar 2026.** **Migration 051 (unit_type + curriculum_context on units) ✅ APPLIED 27 Mar 2026.** All major migrations confirmed applied. Remaining unknown: 028 (student tool sessions).
- ~~**Student login cookie not persisting on Vercel (23 Mar 2026)**~~ **FIXED.** Two stacked bugs: (1) Vercel CDN strips `Set-Cookie` headers from responses with `Cache-Control: public` — Next.js Route Handlers default to `public, max-age=0, must-revalidate`. Fixed by adding `Cache-Control: private, no-cache, no-store, must-revalidate` to all auth routes + global rule in `next.config.ts` for `/api/auth/*`. (2) Migration 041 (`class_students` junction) created an ambiguous PostgREST relationship (PGRST201) — two paths from `students` to `classes` (direct FK + junction table). The nested select `students(... classes(...))` returned HTTP 300 instead of data. Fixed by rewriting `student-session/route.ts` to query each table separately with no nested joins.
- **Class-local editor initial load had 404** — content API route was using `.single()` on class_units query (throws when no row exists for unforked units). Fixed to `.maybeSingle()`. Also added ownership check fallback (tries without `author_teacher_id` filter if first query fails). Deployed 22 Mar 2026.
- **Dashboard 3-button layout needs visual testing** — Manage button uses class color fill with white text. Edit Unit uses outline style. Verify these render correctly across all class color assignments (8 colors in CLASS_COLORS array).
- NM ObservationSnap `classes` table query uses `.eq("author_teacher_id", user.id)` — may need to be `teacher_id` if classes table uses that column for ownership. Needs verification.
- **Archive UI not built** — `is_archived` column exists on classes (migration 033), dashboard filters it, unit detail page shows archived badge, but there's no button for teachers to archive/unarchive a class. Low priority — can be added to class settings or class detail page later.
- Old Own Time components (`src/components/own-time/`) still on disk but unused — safe to delete. Old teacher approve route (`src/app/api/teacher/own-time/approve/`) still on disk but unreferenced — safe to delete.
- ~~No academic integrity safeguards beyond portfolio process documentation~~ BUILT 19 Mar 2026 — MonitoredTextarea + analyzeIntegrity + IntegrityReport. Needs testing + wiring into submission flow.
- Admin test sandbox skeleton endpoint still uses hardcoded model `claude-sonnet-4-20250514` rather than using config's model selection
- ~~Skeleton generation "AI response missing lessons array" intermittent failure~~ HARDENED 19+20 Mar 2026 — `anthropic.ts` `generateSkeleton()` tries multiple nesting patterns and searches for any array containing lesson-like objects. **20 Mar:** dynamic max_tokens scaling (was hardcoded 4096, now up to 8192 based on lesson count — 15-lesson skeletons were likely truncating). Added stop_reason, model, usage stats logging. Error still occurred 20 Mar with "Got keys: []" (empty tool input) — check server logs for `[generateSkeleton]` on next occurrence.
- Test coverage expanding — integrity + timing validation tests added (19 Mar), but still need API route tests, integration tests. **Live browser testing done on timing engine (19 Mar):** admin sandbox generation PASS (workshopPhases, extensions, validation all working), backward compatibility PASS (old units render fine), zero console errors. Test checklist at `docs/timing-engine-test-checklist.md`. PhaseTimelineBar drag/lock/preset interaction not yet tested (needs full wizard flow to generate lesson with workshopPhases, then expand in JourneyLessonCard).
- ~~useToolSession hook built but not yet wired into any toolkit tool component~~ ✅ WIRED 26 Mar 2026 — all 24 tools connected
- Timing validation wired into generate-journey, test-lesson, generate-unit, and regenerate-page. NOT generate-timeline (different data shape — flat activities, not lessons)
- PhaseTimelineBar mounted in JourneyLessonCard (interactive) and unit detail page (read-only MiniPhaseBar). NOT yet in TimelineLessonCard or TimelineBuilder.
- TimingFeedbackPrompt component built but not yet mounted in a post-lesson flow (needs trigger mechanism — after scheduled lesson date or manual "I taught this" button)
- Post-lesson feedback → teacher profile learning pipeline not yet built (stores feedback but doesn't compute adjustment factors for future generation)
- ~~`/toolkit` was missing from middleware public routes, causing auth redirects for unauthenticated visitors~~ FIXED 17 Mar 2026

## Key decisions made
- **AI provider abstraction with fallback chain** — Anthropic → Groq → Gemini, factory pattern
- **Tool use for structured output** — forces JSON schema compliance via tool_choice rather than parsing free text; note: thinking cannot be combined with tool_choice (Anthropic API limitation)
- **Flexible page architecture** — units store content in JSONB; page types: strand, context, skill, reflection, custom
- **Student auth via tokens not Supabase Auth** — nanoid(48) tokens with 7-day TTL for simpler student onboarding
- **Hybrid search over pure vector** — 70/20/10 weighting (vector/BM25/quality) outperforms vector-only
- **Haiku for student-facing, Sonnet for generation** — cost/speed tradeoff; 300-token cap on mentor prevents over-helping
- **Self-healing validation** — invalid AI output gracefully defaults rather than hard failing
- **3-pass analysis for knowledge ingestion** — Structure (Haiku) → Pedagogy (Sonnet) → Design Teaching (Sonnet)
- **Analytics: Plausible or PostHog over GA4** — COPPA/GDPR concerns with tracking minors (ages 11-16)
- **In-memory rate limiting over DB-backed** — resets on Vercel cold start but simplest approach; DB-backed can come later
- **Design Toolkit is framework-agnostic (17 Mar 2026)** — universal phases (Discover/Define/Ideate/Prototype/Test) not MYP-specific, so any design teacher worldwide can use it. Maps to 8+ curricula. This was a critical pivot from the original MYP-only approach.
- **Toolkit at top-level `/toolkit` not under `/tools/`** — gives it its own identity and dark-themed layout separate from the gray `/tools/` layout used by report writer and marking comments
- **Toolkit uses inline styles for dark theme** — the app's Tailwind theme is light; inline styles ensure the dark toolkit theme doesn't leak into other routes
- **Education AI patterns: effort-gate before feedback (17 Mar 2026)** — all interactive toolkit tools must assess student response quality client-side BEFORE choosing a feedback strategy. Low effort gets pushed for specifics (no praise), high effort gets acknowledged + challenged deeper. This is the core differentiator vs Khanmigo (which gives same feedback regardless of effort). Documented in `docs/education-ai-patterns.md`.
- **Prompts are always read-only (17 Mar 2026)** — students can never click a prompt to auto-fill their response. They must always type their own ideas. Matt's explicit decision — "the student should always be forced to enter their own ideas."
- **Client-side effort assessment over AI-based (17 Mar 2026)** — effort level is determined by word count + linguistic markers (reasoning words, specificity markers) on the client, not by the AI. This gives instant feedback (no API latency), is deterministic (no parsing failures), and the effort level is passed to the API so it can adapt its tone.
- **Phase-aware AI feedback (17 Mar 2026)** — AI nudge tone MUST match the design thinking phase. Ideation tools (SCAMPER, brainstorm, etc.) use divergent encouragement ("what else? push further! wilder!"). Evaluation tools (Decision Matrix, PMI, etc.) use convergent analysis ("what are the trade-offs? who does this fail for?"). Critical questions during ideation kill creative flow — this was a live bug in SCAMPER's first iteration.
- **No auto-deal of prompts (17 Mar 2026)** — prompts are hidden until the student writes their first idea. Student must think independently before scaffolding is offered. After first idea, prompt section slides in and student can choose to deal cards. This reinforces effort-first philosophy.
- **Input-first hierarchy (17 Mar 2026)** — on SCAMPER working screen, the textarea is positioned immediately after the step header. Prompts/scaffolding sit below as secondary support. Example is collapsible ("See an example" toggle) to reduce text clutter. The primary action (writing) should always be above the fold.
- **Four interaction shapes for toolkit tools (17 Mar 2026)** — not all 42 tools should be interactive the same way. Step Sequence (SCAMPER-like steps), Canvas (spatial/visual regions), Comparison Engine (criteria-based scoring with reasoning), Guided Composition (structured writing with coach). ~14 tools are better as templates. Shared components extracted from SCAMPER speed up all future builds. See `docs/ideas/toolkit-interactive-tools-plan.md`.
- **Per-step AI rules are the key differentiator (18 Mar 2026)** — each tool has step/quadrant/column-specific AI rules injected into the system prompt. Six Hats has `hatRules` + `hatTone` per hat; PMI has `colRules` + `colTone` per column; Five Whys has depth-detection logic; Empathy Map has `quadRules` + `quadTone` per quadrant. This means the AI adapts its entire personality per step, not just per tool. No other edtech product does this.
- **Depth detection for Five Whys (18 Mar 2026)** — the AI nudge system prompt includes the student's previous answers chain and explicitly checks if the new answer is "at the same level" (sideways) or "one layer deeper" (good root cause analysis). This is the most pedagogically sophisticated nudge in the toolkit.
- **Empathy Map Feels quadrant pushes contradictions (18 Mar 2026)** — the AI specifically asks students to identify CONTRADICTORY emotions ("excited AND anxious"), pushing past simple happy/sad to nuanced emotional states. This is based on empathy mapping best practice from IDEO and Stanford d.school.
- **Consistent 3-screen architecture (18 Mar 2026)** — all interactive tools use the same intro → working → summary flow. Intro: challenge input + how-it-works. Working: step nav + input-first + prompts + nudge + ideas list. Summary: all steps + AI synthesis + copy. This consistency means students learn the pattern once and can use any tool.
- **All 3 interaction shapes proven (19 Mar 2026)** — Step Sequence (10 tools), Comparison Engine (Decision Matrix), Guided Composition (How Might We). The architectural patterns are validated and can scale to remaining tools.
- **Student toolkit access: two modes, one component (19 Mar 2026)** — Embedded mode (teacher assigns tool on unit page, saves as response, flows to portfolio) and Standalone mode (student opens from floating launcher or nav, saves independently). Same React component, different persistence. Completed sessions are editable (reopen to add ideas) or versionable (start fresh as v2, v3...). Versions show iteration/growth in portfolio — exactly what MYP wants.
- **Public toolkit stays free/unauthenticated (19 Mar 2026)** — `/toolkit` routes remain the free lead-gen tool for teachers. Student versions require auth and persist data. Same components render in both modes.
- **Silent integrity monitoring, not visible to students (19 Mar 2026)** — MonitoredTextarea captures writing behavior with zero visible indicators. Students see a normal textarea. Metadata stored alongside responses in JSONB. Teachers see the IntegrityReport on the grading dashboard. This is a design decision: monitoring is for academic integrity evidence, not surveillance theatre.
- **6-rule integrity scoring over AI-based (19 Mar 2026)** — Human Confidence Score uses deterministic rules (paste ratio, bulk entry, typing speed, editing rate, focus loss, minimal time) not AI classification. Deterministic = explainable to parents/admin, no API cost, instant. Threshold: ≥70 = likely independent, 40-69 = review recommended, <40 = flagged.
- **Timing validation as post-generation guard, not prompt-only (19 Mar 2026)** — Don't just tell the AI to follow Workshop Model in the prompt; validate the output afterward and auto-repair. 8 rules checked, 6 auto-repaired. Separate from generation so prompt changes don't break validation. Schema now collects `workshopPhases` + `extensions` so the AI has the right output structure.
- **Lazy session creation for toolkit persistence (19 Mar 2026)** — useToolSession creates the session on first `updateState()` call, not on mount. Prevents empty session records when students browse without engaging. Debounced auto-save at 500ms coalesces writes.
- **Floating toolkit launcher on all student pages (19 Mar 2026)** — not just during units. Students sometimes need a tool quickly for something outside the current assignment.
- **Fan menu: vertical stack over radial arc (18 Mar 2026)** — radial arc with SVG connecting lines was ugly and clipped off-screen. Replaced with vertical pill stack (phases upward from FAB) + horizontal tool slide-out. Uses Framer Motion springs for entrances/exits. Simpler, more readable, no positioning bugs.
- **Design guidelines as canonical reference (19 Mar 2026)** — `docs/design-guidelines.md` captures 42 principles across 5 categories. Feeds the project dashboard. Will split into shared + project-specific layers when porting to Makloom.
- **Workshop Model is non-negotiable (19 Mar 2026)** — Every AI-generated lesson MUST follow the 4-phase Workshop Model: Opening (5-10 min) → Mini-Lesson (max 1+age min) → Work Time (≥45% of usable time) → Debrief (5-10 min, structured protocol). This is backed by research from d.school, PBLWorks, ASCD, Cult of Pedagogy, Project Zero. Work Time is one sustained block — never fragment it into small activities.
- **1+age rule for instruction cap (19 Mar 2026)** — Maximum direct instruction minutes = 1 + average student age. Year 7 (age 12) = 13 min. Year 10 (age 15) = 16 min. Simpler and more research-backed than the old `maxHighCognitiveMinutes` lookup table. Formula from PBLWorks and cognitive science research.
- **Always use usable time, never raw period (19 Mar 2026)** — The legacy path in `buildTimingBlock()` used to generate content for the full period length when no `TimingContext` was provided. Now it always constructs a default context (theory lesson, 3-min transition). A 60-min workshop lesson has only 41 usable minutes — the AI must generate for 41, not 60.
- **Extensions are mandatory on every lesson (19 Mar 2026)** — Every AI-generated lesson must include 2-3 extension activities for early finishers, indexed to the current design phase. Investigation → deeper research. Ideation → creative variation. Prototyping → alternative materials. Evaluation → edge-case rigor. Extensions are productive deepening, not busywork.
- **Server-side timing validation with auto-repair (19 Mar 2026)** — Don't just prompt the AI to follow rules; validate the output afterward and fix what it gets wrong. 8 rules checked, 6 auto-repaired (missing phases, over-cap instruction, under-floor work time, missing debrief, time mismatch, missing checkpoints). Separate from generation so prompt changes don't break validation.
- **Teacher timing modification via Phase Timeline Bar (19 Mar 2026)** — Teachers drag phase boundaries to redistribute time. Lock phases to prevent auto-resize. One-click presets for common patterns (Balanced, Hands-On Heavy, Instruction Heavy, Critique Session). Post-lesson feedback feeds the learned profile. Teachers know their class better than the AI does — give them the controls.
- **Open Studio replaces Own Time entirely (20 Mar 2026)** — not an extension or parallel system. Migration 029 drops the old tables. Per-unit scoping (not per-class like Own Time). Teacher-only unlock for MVP (auto-calculated criteria later). Configurable check-in interval (5-30 min). Per-unit reset with optional carry-forward.
- **AI mode switching via DB flag, not conversation analysis (20 Mar 2026)** — `open_studio_status.status === 'unlocked'` determines which system prompt is used. Clean, deterministic switch. The Design Assistant checks the DB at response generation time and picks guided or Open Studio prompt.
- **Drift detection with 3-level escalation (20 Mar 2026)** — gentle nudge → direct question → silent flag to teacher. Auto-revocation after 2 consecutive sessions with silent drift flags. Escalation resets per session. Teachers see drift flags in the session log.
- **5 interaction types for Open Studio AI (20 Mar 2026)** — student_message (studio critic tone), check_in (periodic wellness check), drift_check (escalating redirects), documentation_nudge (push to document process), alignment_check (MYP criterion alignment). Each has distinct system prompt instructions.
- **Check-in rotation pattern (20 Mar 2026)** — regular check-in → documentation nudge every 3rd → alignment check every 5th. Prevents monotony. All run on the configurable timer interval.
- **Teaching Mode: polling over Supabase Realtime (20 Mar 2026)** — 8-second polling to `/api/teacher/teach/live-status` for student progress. Simpler than Realtime subscriptions, good enough for classroom use (teacher sees updates within 8s). Can upgrade to Realtime later if needed.
- **Teach Now on main dashboard (20 Mar 2026)** — one-click teaching launch from the main teacher dashboard. Each active unit shows as a card with progress ring. Links to `/teacher/teach/[unitId]?classId=[classId]` so the right class is pre-selected. Reduces friction from 3 clicks to 1.
- **Projector view as separate window, not iframe (20 Mar 2026)** — teacher opens projector view in a new window/tab and drags to projector screen. Dashboard syncs via `postMessage`. Simpler than building a dual-panel layout. Teacher controls stay private on their laptop.
- **China student auth: anonymous by default (20 Mar 2026)** — class code + student-chosen display name, no real names stored in platform. Keeps data anonymized under PIPL. Teacher maintains any name mapping offline. Real-name support requires China-hosted infrastructure (Alibaba Cloud / Tencent Cloud) and standard contractual clauses — a "when you have revenue" problem.
- **Presentation-ready output is a generation problem, not UI (20 Mar 2026)** — the teaching cockpit can only display what the wizard generates. Extending `workshopPhases` schema so each phase carries displayable assets (hook, vocab, checkpoint questions, protocols) is the real fix. Parked on roadmap Phase 4.
- **Projector classroom tools: picker + timer + groups + noise (20 Mar 2026)** — these are the tools teachers tab away for every lesson. Building them into the projector view keeps teachers inside StudioLoom. Noise meter (mic-based volume indicator) is the "wow" feature. Skip: stopwatch, behaviour tracking (ClassDojo territory), seating chart.
- **Pop art visual identity for NM (20 Mar 2026)** — NM components have a deliberately different visual language from the rest of StudioLoom. Hot pink (#FF2D78), electric yellow (#FFE135), cyan (#00D4FF) with bold black borders, halftone dot patterns, box shadows, Arial Black font. This makes NM feel like a distinct "layer" overlaid on the platform — students instantly recognise NM checkpoints. Inspired by Andy Warhol's pop art.
- **NM starts with Agency in Learning only (20 Mar 2026)** — the Melbourne framework has 7 competencies but only Agency in Learning has fully defined elements + progressions in the constants file. Other competencies return empty element arrays. Expand as Matt gets the full Melbourne kits.
- **NM config stored as JSONB on units table (20 Mar 2026)** — `nm_config` column on `units` table stores `{ enabled, competencies, elements, checkpoints: { [pageId]: { elements } } }`. Per-unit, not per-class. Teacher picks which elements to track and assigns them to checkpoint pages via 3-step wizard.
- **NM page_id is TEXT not UUID (20 Mar 2026)** — discovered that StudioLoom page IDs are nanoid(8) strings, not UUIDs. Migration 032 fixes the column type. This is a recurring pattern — any table referencing page IDs must use TEXT.
- **NM observation accessible from Teaching Mode (20 Mar 2026)** — teacher can observe a student's competency in real-time during class via a button on each student card in the teaching cockpit. This is the intended workflow: watch a student, tap NM, rate, move on. Much faster than navigating to the unit page.
- **Unit-as-template architecture / Canvas Blueprint pattern (20 Mar 2026)** — Units are content templates, not per-class instances. `class_units` junction table stores per-class configuration overrides (NM config, future: timing, Open Studio settings). Inspired by Canvas LMS Blueprint Courses. NM config inheritance: `class_units.nm_config` → `units.nm_config` → `DEFAULT_NM_CONFIG`. This means a teacher can enable NM for one class but not another, using the same unit content.
- **NM config belongs on class-unit settings page, not unit page (20 Mar 2026)** — Matt's explicit feedback: "I don't know what this little NM dropdown does, I don't want to control it at this high level." NM is a per-class pedagogical decision. Unit page shows WHAT classes use this unit. Class-unit page controls HOW they use it. Clean separation of content (unit) vs configuration (class-unit).
- **Assigned Classes section on unit detail page (20 Mar 2026)** — Shows which classes are assigned to a unit with student count, class code, NM badge, and archived state. Each card links to `/teacher/units/[unitId]/class/[classId]` for per-class settings. This makes the unit→class relationship visible and navigable. Previously, the only way to see class assignments was via the dashboard.
- **Auth helper pattern for teacher routes (20 Mar 2026)** — `src/lib/auth/verify-teacher-unit.ts` centralises 3 common auth checks: teacher owns unit, teacher owns class, get NM config with fallback chain. Replaces duplicated auth+query logic across NM routes. Pattern: `createServerClient` for auth only, `createAdminClient` for all DB operations (bypasses RLS).
- **Student route links must include sub-path (20 Mar 2026)** — Student unit route has no index page at `/unit/[unitId]` — only `/narrative` and `/[pageId]` sub-routes exist. Links to `/unit/${unitId}` cause Next.js prefetch 404s. Always use `/unit/${unitId}/narrative` as the fallback link target.
- **Migration 033 is additive-only by design (20 Mar 2026)** — Only adds columns and indexes, never modifies or drops existing columns. This means the migration can be applied safely and code can be reverted independently if issues arise. The new columns default to NULL so existing data is unaffected.
- **Effort-gated reflections over tickbox (20 Mar 2026)** — Old reflections (confidence sliders, checklists, empty textareas) were "tickbox exercises" requiring zero thought. Replaced with unified written-response requiring 12+ meaningful words (excluding filler). Confidence slider only appears AFTER writing threshold met. Sentence starters guide quality ("Start with 'I noticed that...'", "Be specific — name the thing", "Connect this to something you actually did"). Effort tags provide instant visual feedback. This applies the same effort-gating pattern used in toolkit tools to the core lesson flow.
- **Comic strip over chat bubbles for Discovery (20 Mar 2026)** — First attempt at visual DiscoveryFlow was styled chat bubbles with comic-style borders. Matt's feedback (with reference image of real illustrated comic) showed he wanted actual scene panels with illustrations. Second iteration added SVG scene illustrations per step, detailed character drawings (mentor + student), thick panel borders, speech bubble tails, cinematic step transitions. Scott McCloud's "Understanding Comics" principles: panel closure (reader fills gaps between panels), progressive reveal (panels animate in with spring physics), visual storytelling. The comic strip replaces the two-column chat+profile layout entirely.
- **No side profile panel in Discovery (20 Mar 2026)** — The old DiscoveryFlow had a right column showing "Start sharing to build your profile" that was confusing (Matt asked "what is the column on the right?"). Removed entirely. Profile data (strengths, interests, needs) now appears inline as ProfileReveal cards within the comic strip flow when the student transitions between phases. Bottom bar shows project statement + archetype badge. Simpler, more focused, no dead space.
- **Sticky JourneyMap in Open Studio (20 Mar 2026)** — The 4-phase journey map (Discovery → Planning → Working → Sharing) is always visible at top of Open Studio chat via `position: sticky`. Auto-compacts when scrolled (shrinks to dot indicators with current phase label). Frosted glass background with blur. This ensures students always know where they are in the process without scrolling up.
- **Sticky top nav on student lesson pages (20 Mar 2026)** — Dashboard button was buried in the hero gradient as faint white text — invisible to students. Moved to a sticky top bar with frosted glass, always visible. Includes page counter, hamburger menu for mobile sidebar, and prominent Dashboard button. Students can always get back to their dashboard in one click.
- **Discovery redesign: story not interview (21 Mar 2026)** — Research-backed pivot. Current Discovery is an interrogation in costume. New approach: mentor tells stories first (campfire effect), student responses to scenarios reveal their profile indirectly. "What do you actually do when your friend needs help?" reveals strengths without asking "What are your strengths?" Based on adolescent psychology research: direct questioning is the #1 trust killer with teens. Provocation over interrogation. Mentor vulnerability before student vulnerability. Every panel is a moment, not a message.
- **Choose Your Mentor before Discovery (21 Mar 2026)** — 5 selectable mentors (Kit/Sage/River/Spark/Haven) with distinct personalities, voices, visual designs, and teaching approaches. Same 5-chapter flow, same data extraction, different experience. The mentor choice itself is a data point — which personality a student gravitates toward reveals something about them before they've answered a question. Stored in DiscoveryProfile. Feeds into AI behaviour for the rest of Open Studio (the system knows not just WHAT the student said but HOW they chose to be spoken to).
- **Student character not illustrated (21 Mar 2026)** — The student is the camera/reader, not a drawn character. Any illustrated student would feel wrong to half the audience. Student speech bubbles rise from the bottom of panels — their voice entering the scene. Graphic novel convention: protagonist's perspective shown through the camera, not through a drawn character.
- **Environmental panels shared, character panels per-mentor (21 Mar 2026)** — The door, collection wall, window, crossroads corridor, and rooftop are the same for all mentors (shared assets). Only character images (~6 per mentor), dialogue, voice, and AI prompts differ. This keeps the production scope manageable: ~45 total images, not 5× everything.
- **Two-layer audio: narration + dialogue (21 Mar 2026)** — Inner monologue (Kit's private thoughts, Max Payne style) plays automatically as panels scroll into view. Dialogue matches speech bubbles. Narration builds character depth the student can't get from text alone ("They don't see it yet — but I do. That instinct they described? That's wiring."). ElevenLabs TTS for pre-recorded clips (~45 per mentor), real-time generation for AI-response panels.
- **Provocation-based strength discovery (21 Mar 2026)** — "Your best friend is panicking, big project due tomorrow, 2 hours left. What do you actually do?" A maker grabs materials. A researcher opens tabs. A leader draws a plan. The student reveals their archetype without being asked to self-categorise. Same principle for interests: "What annoys you?" surfaces authentic interests better than "What are you passionate about?" Passion is abstract; irritation is concrete.
- **Pace-only student feedback (21 Mar 2026)** — Of all possible post-lesson metrics (pace, difficulty, confidence, comprehension, engagement), pace is the only one worth collecting via self-report. Comprehension is better measured from behavioral data (response quality, time spent, integrity metrics). Difficulty and confidence are too abstract for students to self-assess reliably. Pace is concrete ("was this too fast or too slow?") and directly actionable for the timing model. One tap, done. Data flows into `lesson_feedback.feedback_data.pace` → aggregated as `pace_distribution` → feeds timing model for future lesson generation.
- **Student feedback uses student auth, not teacher auth (21 Mar 2026)** — StudentFeedbackPulse posts to `/api/student/pace-feedback` with `requireStudentAuth` (cookie token session). The old route used `requireTeacherAuth` which returned 401 for students. Lesson: any component rendered on student pages must use student auth routes.
- **School calendar at teacher level, not school-admin (21 Mar 2026)** — Researched ManageBac (school-admin sets calendar, terms cascade to classes) and Toddle (similar school-level approach). StudioLoom's target user is a solo teacher, not a school admin. Simplified to per-teacher calendar setup in Teacher Settings. Quick templates (4 Terms, 2 Semesters, 3 Trimesters, Custom) make setup fast. Term assignment happens on the class-unit settings page, not the unit page — consistent with the unit-as-template architecture.
- **usePageData must use useMemo for stable references (21 Mar 2026)** — Bare object literals in React hooks create new references on every render, even when the underlying data hasn't changed. This triggers dependent useEffects (like usePageResponses) that reset state. The fix is `useMemo` + ref guards (`loadedPageRef` to only load once per page). This is a general React pattern: any hook returning an object that will be used in a dependency array must be memoized.
- **Anchor-based modular arithmetic for rotating cycles (21 Mar 2026)** — Cycle day = `((weekdaysBetween(anchor, target) % cycleLength) + anchorCycleDay - 1) % cycleLength + 1`. Only counts weekdays (Mon-Fri). Excludes dates in `excluded_dates` array. Optional per-term reset (re-anchors to Day 1 at start of each term). This handles 5/6/8/10-day cycles correctly across weekends, holidays, and term breaks.
- **iCal import for timetable, not LMS-specific integrations (21 Mar 2026)** — Rather than building ManageBac/PowerSchool/Toddle-specific API integrations, accept standard .ics files that any LMS can export. Lightweight RFC 5545 parser extracts VEVENT entries. Holiday detection via all-day events + keyword matching. Class matching via event SUMMARY → teacher's class names. Universal approach works for any school system.
- **Teacher settings: separate Workshop & Equipment tab (21 Mar 2026)** — Workshop spaces, tools & machines, and software sections moved from School tab to dedicated Workshop tab with wrench icon. Keeps school-level config (calendar, timetable) distinct from physical resources. Matt's request — workshop equipment is a separate concern from school structure.
- **Schedule overrides as JSONB on class_units, not a new table (22 Mar 2026)** — Lesson-level teacher adjustments (extra sessions for overtime, skip dates, notes) stored as `schedule_overrides` JSONB on the existing `class_units` junction table. Right granularity (per class-unit), avoids a new table + migration complexity. Shape: `{ extra_sessions: { pageId: N }, skip_dates: ["YYYY-MM-DD"], notes: { pageId: "text" } }`. Extra sessions default to 0 = 1 period per lesson; setting to 1 means "continues next class" (2 periods total).
- **Visual timetable grid over row-by-row entry (22 Mar 2026)** — TimetableGrid component presents Day 1-N columns × Period rows. Click empty cell to add class meeting (dropdown), click filled cell to remove. Much faster than the original approach of dropdowns per row. Color-coded by class (8-color palette). Quick-add row below grid for keyboard-first teachers. Both approaches (visual grid and quick-add) available simultaneously.
- **LessonSchedule uses server-side date computation (22 Mar 2026)** — Rather than duplicating cycle engine logic on the client, LessonSchedule calls the existing `/api/teacher/schedule/lessons?mode=next` endpoint to get the next N class dates. This ensures the cycle day calculation, excluded dates, and weekday-only logic stays in one place (cycle-engine.ts). The component requests enough dates to cover all sessions including extras from "continues" overrides.
- **Calendar templates always visible, not one-time setup (22 Mar 2026)** — SchoolCalendarSetup quick templates (4 Terms, 2 Semesters, 3 Trimesters, Custom) were hidden after first save, trapping teachers who deleted their terms. Fixed: templates always visible with dynamic label ("Quick Setup" → "Change Structure"). Teachers can change their term structure anytime without workarounds.
- **Copy-on-write forking for per-class content (22 Mar 2026)** — `class_units.content_data` defaults to NULL (inherits master). On first edit, `ensureForked()` deep-copies `units.content_data` into `class_units.content_data` with `forked_at` timestamp and `forked_from_version`. Same inheritance pattern as NM config (`class_units.nm_config` → `units.nm_config` → default). Resolution chain: `resolveClassUnitContent(masterContent, classContent)` returns `classContent ?? masterContent`. Pure function, no DB access.
- **Page ID preservation is a critical invariant during forking (22 Mar 2026)** — Progress records, NM checkpoints, due dates, schedule overrides, and student responses all reference page IDs. The deep copy in `ensureForked()` preserves all page IDs from master. Never regenerate or remap page IDs during fork. If master adds new pages later, they appear in master but not in the fork (fork is a snapshot).
- **Always use `.maybeSingle()` not `.single()` for optional rows (22 Mar 2026)** — Supabase `.single()` throws a PostgreSQL error when no row matches. `.maybeSingle()` returns null gracefully. This bit us on the class_units content query (unforked units have no content_data row match). General rule: use `.single()` only when you KNOW a row must exist (e.g., by ID after an insert). Use `.maybeSingle()` for any lookup that might legitimately return zero rows.
- **Dashboard 3-button layout: Teach / Manage / Edit Unit (22 Mar 2026)** — Replaced 5 buttons (Teach/Progress/Grade/Edit/Settings) with 3. "Manage" (class-colored solid fill) links to tabbed class-unit page (Overview/Progress/Grade/Safety tabs). "Edit Unit" (gray outline) links to class-local content editor. Reduces visual clutter on dashboard unit rows.
- **Tabbed Manage Class page over separate settings page (22 Mar 2026)** — Class-unit page at `/teacher/units/[unitId]/class/[classId]` now has tabs: Overview (NM config, schedule, settings), Progress (links to progress view), Grade (links to grading), Safety (badge management). Previously just a settings page. Tabs keep related functionality together without deep navigation.
- **Class-local editor as separate page, not inline (22 Mar 2026)** — Content editing for a class fork lives at `/teacher/units/[unitId]/class/[classId]/edit`, not embedded in the Manage page. Editing is a focused task that benefits from its own full-screen space. Shows fork status badge (amber "Class fork" vs blue "Using master template").
- **Content resolution wired everywhere class context exists (22 Mar 2026)** — Every route/component that reads `unit.content_data` in a class context must use `resolveClassUnitContent()`: class-unit settings page, Teaching Mode dashboard, projector view, student unit route, student units listing route. Missing any one breaks the fork illusion.
- **Teaching Mode stores class content in ref, not state (22 Mar 2026)** — `classContentRef` (useRef) stores the per-class content map loaded at initial fetch. Resolved per-class in render section. Avoids re-fetching when teacher switches between classes in the dropdown.
- **All auth routes must set Cache-Control: private on Vercel (23 Mar 2026)** — Vercel CDN strips Set-Cookie from responses with Cache-Control: public. Next.js Route Handlers default to public. Added explicit `private, no-cache, no-store, must-revalidate` to student-login, student-session, and LTI launch routes. Also added a global header rule in `next.config.ts` for `/api/auth/:path*` as belt-and-suspenders. This is production-only (not visible in local dev).
- **Separate queries over nested joins when multiple FK paths exist (23 Mar 2026)** — After migration 041 added `class_students` junction alongside `students.class_id`, PostgREST couldn't auto-resolve nested selects (PGRST201 ambiguity). Rewrote `student-session/route.ts` to query student_sessions → students → class_students → classes as separate sequential queries. Simpler than PostgREST hint syntax and more robust against future schema changes.
- **Student-class junction with additive migration strategy (23 Mar 2026)** — Migration 041 keeps `students.class_id` for backward compatibility while adding `class_students` junction for multi-class enrollment. New code uses junction (with legacy fallback), old code still works. `author_teacher_id` on students tracks who owns the student record (backfilled from `classes.teacher_id`). `class_id` made nullable. This is the same additive pattern used for unit-as-template (migration 033).
- **Lesson editor as component library, not monolith (24 Mar 2026)** — The old class-local editor was a 545-line monolith (inline accordion, manual save button, no undo). The new approach splits into 12 focused components: LessonEditor orchestrator, useLessonEditor state hook, useAutoSave, UndoManager, LessonSidebar, ActivityBlock, ActivityBlockAdd, PhaseSection, LessonHeader, ExtensionBlock, InlineEdit, barrel index. The page file is now ~30 lines (thin wrapper). This separation means individual pieces can be reused (e.g., ActivityBlock in a student preview, PhaseSection in Teaching Mode). Spec at `docs/specs/lesson-editor-phase-0.5.md`, usage guide at `src/components/teacher/lesson-editor/USAGE-GUIDE.md`.
- **activityId backfill required before drag-to-reorder (24 Mar 2026)** — Student responses are keyed as `section_N` (index-based). If activities are reordered, indexes shift and responses become misattributed. The backfill script (`scripts/backfill-activity-ids.mjs`) assigns stable `activityId` to every section and migrates response keys from `section_N` to `activity_<id>`. **MUST run before enabling drag-to-reorder in production.** Safe to run multiple times (idempotent — skips sections that already have activityId).
- **normalizeContentData() is mandatory for any code reading pages (24 Mar 2026)** — StudioLoom has 4 content versions. Direct `.pages` access only works for v2. The useLessonEditor hook was initially broken on Vercel because it skipped normalization. Always import from `@/lib/unit-adapter` and normalize before accessing `.pages`. See Lesson Learned #14.
- **# icon is the AI brand mark (24 Mar 2026)** — The `#` symbol (grid/hash) represents AI assistance throughout StudioLoom. Used as a purple circle button on text fields in the lesson editor (AITextField component). Consistent visual language: whenever students or teachers see the #, it means "AI can help here."
- **Cross-container DnD via HTML5 API + React context, not dnd-kit (24 Mar 2026)** — Framer Motion Reorder handles within-list activity reordering; HTML5 Drag and Drop API handles cross-container dragging (palette → phase). A React context (`DndContext`) bridges the two by tracking drag state so DropZone components can show/hide and highlight during drags. No additional library needed. Key compatibility note: `motion.button`'s `onDragStart` type conflicts with HTML5 DnD events — must use plain `div` for draggable palette blocks.
- **Student leveling: quality-weighted, not volume-based (24 Mar 2026)** — Designer Level system rewards depth and skill demonstration, not grinding. A student doing excellent work on 2 units should level faster than mediocre work on 5. No public leaderboard for ages 11-16 (social comparison anxiety). Levels persist across years. Composite from criterion scores (primary signal) + toolkit depth + safety badges + reflection quality + Open Studio productivity + NM ratings + portfolio richness. Depends on grading page being functional. Full spec in `docs/roadmap.md` Phase 1 Gamification section.
- **Year planner: week-based grid, not day-based (25 Mar 2026)** — Teachers think in weeks, not days. A day-based grid would be too zoomed in for a year view. Lesson count is derived from cycle engine (start_date + lesson_count + timetable → end date), never stored as end date. Only 2 new columns needed on `class_units`: `planned_start_date` and `planned_lesson_count`.
- **Curriculum coverage as overlay, not primary view (25 Mar 2026)** — The year planner is useful even without curriculum tagging. Coverage adds value but shouldn't be required. Framework data is seeded (MYP, GCSE, ACARA, A-Level), not user-authored (that's Layer 2). Unit tagging is manual + auto-backfilled from existing content_data criterion arrays.
- **Materials on content_data, not a separate table (25 Mar 2026)** — Materials are unit content: they fork with the unit (class A uses MDF, class B uses cardboard for the same unit), travel with exports, and don't need independent querying. Purchasing aggregation is computed at read time from class_units × class size. Only the "ordered" checkbox state needs its own column (`purchasing_state` JSONB on class_units). Quantity stored per-student ("2 sheets each"), system multiplies by class size for the shopping list.
- **Uploaded content should become editable units, not just RAG data (25 Mar 2026)** — The knowledge base pipeline extracts analytical data (LessonProfiles, chunks, embeddings) but doesn't produce editable content. The Lesson Plan Converter should produce BOTH: a proper `content_data` unit the lesson editor can open AND tagged activity blocks for the reusable block library. Dual output from one upload.
- **Activity block library grows from uploads, not pre-built templates (25 Mar 2026)** — Rather than curating a catalog of activity templates, the library grows organically from real teaching content. Every upload (lesson plan, textbook, scheme of work) extracts individual activities as reusable blocks. The lesson editor's "Import from..." search queries two sources: (a) activities from the teacher's own units, (b) activities extracted from uploaded materials. AI-powered embedding search finds relevant blocks by description. This means even a textbook with one great activity makes that activity available as a draggable block.
- **Teaching Mode quick-edit: slide-out panel, not popover or page navigation (25 Mar 2026)** — Teachers mid-class need more than field tweaks (they may add activities, reorder, adjust scaffolding). A popover is too cramped; a full page navigation breaks flow. A slide-out panel (~400px from right) gives enough room while keeping the student grid visible. Reuses existing lesson editor components (ActivityBlock, InlineEdit, AITextField, PhaseSection, ActivityBlockAdd) — they're already modular. Saves via same fork-on-write content API, syncs to projector via existing postMessage. Scoped to current lesson only.
- **"Import from..." over "bigger block catalog" (25 Mar 2026)** — Considered a large browsable block catalog in the editor sidebar but rejected due to choice paralysis. 6 category templates (discussion, hands-on, research, reflection, presentation, assessment) stay as the primary fast-add mechanism. "Import from..." is the power feature for teachers who want to pull specific activities from other lessons or uploaded materials. Keeps the editor fast and focused.
- **Edit Mode Toggle: "who sees this change?" not "which version am I editing" (25 Mar 2026)** — Teachers don't want to think about forks, masters, or versions. The editor has a simple toggle: "All classes" (writes to master template, all non-customized classes see it) vs "This class only" (current fork behavior). Plus "Apply to All Classes" button to promote a class fork to master. Version history shown as a timeline in the sidebar (date + context, not version numbers). No developer terminology anywhere. Spec: `docs/specs/unit-editing-navigation-spec.md`.
- **Dashboard buttons: 2 not 3 (25 Mar 2026)** — "Edit Unit" button was navigating to the settings/manage page (term picker, schedule, NM config), not the lesson editor. Reduced to 2 buttons: Teach (Teaching Mode) + Edit (lesson editor). The manage/settings page is a setup task accessed via ⚙ link inside the editor sidebar. Teachers set up terms and schedules once per semester — this doesn't belong in the daily workflow.
- **Class model: semester container with archived classes (25 Mar 2026)** — Matt's school locks students to classes per semester/year, then archives the class and creates new ones. This is the most common model (ManageBac, Toddle, PowerSchool, Canvas all work this way). The `class_students` junction table supports both locked and fluid enrollment. No architecture change needed — the model is correct.
- **Settings are setup, not daily work (25 Mar 2026)** — Term assignment, timetable config, NM setup, safety badge management — these are all done once per semester. They should NOT be in the teacher's daily path (dashboard buttons). Accessible via ⚙ gear link in the editor sidebar or from the unit detail page's assigned classes section.
- **Smart subject detection with 3-level fallback (25 Mar 2026)** — Student unit cards need framework-colored headers (DESIGN=teal, SERVICE=pink, PP=purple, PYPX=amber) but `class_subject` DB field is often null. Solution: `detectSubject()` checks 3 sources in priority order: (1) `class_subject` from DB, (2) `class_name` keywords, (3) `unit.title` keywords. SUBJECT_MAP matches against padded lowercase strings with keyword arrays. Deterministic gradient fallback based on title hash when no keywords match. This means cards get correct colors even without explicit DB setup — teacher just needs a sensible class name.
- **Multi-class student dashboard: no single class name (25 Mar 2026)** — Students enrolled in multiple classes via `class_students` junction shouldn't see a single class name in their dashboard header. Removed it. Each unit card shows its own subject/framework label via the colored header strip. Student identity (name) stays, schedule info (next class pill) stays — class affiliation is per-unit, not per-student.
- **Open Studio tab in Class Hub for progressive feature access (25 Mar 2026)** — Rather than building a separate Open Studio management page, added it as a tab in the existing Class Hub (`/teacher/units/[unitId]/class/[classId]`). Uses the existing `OpenStudioClassView` component. This establishes the Class Hub as the central place for ALL per-class-unit management: Overview, Progress, Grade, Badges, New Metrics, Students, Open Studio.
- **Discovery Engine: 8 stations not 5 chapters (26 Mar 2026)** — The old comic-strip approach (5 chapters of illustrated panels + voiceover) was too passive — students watched more than they participated. The new 8-station model has students actively interacting at every point (binary choices, drag sorts, text prompts, slider scales, scene clicks, card sorts). Kit reacts to EVERY input. Old spec docs (comic-strip-spec, mentor-system, voiceover-script) in `docs/open studio/` are superseded by `docs/specs/discovery-engine-build-plan.md`.
- **Discovery Engine: Kit only, not 5 mentors (26 Mar 2026)** — The 5-mentor system (Kit/Sage/River/Spark/Haven) was over-engineered. Each mentor needed ~45 unique dialogue lines, separate AI system prompts, and per-mentor images. Reduced to Kit only — one well-developed character is better than 5 shallow ones. The mentor choice data point was interesting but not worth 5× the content creation.
- **Discovery Engine: two modes, one journey (26 Mar 2026)** — Mode 1 (Design) and Mode 2 (Service/PP/PYPx) share the same 8 stations but diverge at critical points: Kit's intro framing, template door scope (project-scale vs journey-scale), and entry context (post-lessons vs unit start). The shared stations mean one codebase, not two parallel Discovery experiences.
- **Discovery Engine: free-text irritation is the highest-quality signal (26 Mar 2026)** — When a student writes their own frustration (instead of picking from a list), it's emotional, self-generated, and specific. AI-analysed with Haiku, weighted at 0.20 (highest individual station weight). Pre-written irritation selections are only weighted at 0.05. This creates a strong incentive in the scoring to write your own — but the system works either way.
- **Discovery Engine: every input gets a response (26 Mar 2026)** — Dead-end audit found 19 places where students gave input and got silence. All now have Kit responses — templates for deterministic inputs (selections, sliders, sorts), Haiku calls for free-text (panic scenario, irritation, problem description). The fear card responses are the most important — 5 genuine mentor-quality paragraphs that normalize the fear and offer concrete reframing.
- **Discovery Engine: content pools editable but hardcoded first (26 Mar 2026)** — All content (binary pairs, scenarios, value cards, etc.) lives in TypeScript files for v1. A future teacher content control panel uses the same override chain pattern as `resolveClassUnitContent()` (hardcoded → teacher → class). This means v1 content files automatically become system defaults when the control panel is built. Zero migration needed.
- **Discovery as student profiling backbone, not just onboarding (26 Mar 2026)** — The DiscoveryProfile is the richest student data StudioLoom collects (archetype, working style, interests, fears, self-efficacy, values). Phase A: wire into Design Assistant system prompt so the AI mentor knows the student (~2-3 days, zero new dependencies). Phase B: adaptive flow for returning students — skip known stations, only do problem-finding + door selection on subsequent units (first unit 45 min, second 20 min, third 10 min). Phase C: merge with passive signals into unified StudentLearningProfile for cross-platform intelligence. Discovery writes, everything else reads. Full spec in `docs/roadmap.md` → Discovery Intelligence Layer.
- **Class Gallery as digital pin-up crit, not social media (26 Mar 2026)** — Gallery rounds are teacher-created, time-bounded events tied to a unit+class. Students share portfolio snapshots (not arbitrary uploads), review peers using structured formats (comment/PMI/two-stars-wish), and only see feedback AFTER completing minimum reviews (effort-gated). This is a pedagogical tool for critique culture (Ron Berger's "Austin's Butterfly"), not a social feed. Anonymous option controlled by teacher. No likes, no followers, no algorithmic ranking.
- **Effort-gating for peer review is non-negotiable (26 Mar 2026)** — Students cannot see feedback on their own submission until they've completed the minimum number of peer reviews. This prevents the free-rider problem and ensures every student engages with others' work before receiving validation on their own. Same effort-gating pattern as toolkit tools and reflections.
- **3 review formats, not free-form (26 Mar 2026)** — Structured review formats (comment with sentence starters, PMI, Two Stars & a Wish) produce better peer feedback than open-ended text boxes. Each format has specific UI and sentence starter scaffolding. Teacher picks the format per round. Future: allow custom rubric-based formats.
- **Gallery submissions are portfolio snapshots, not new uploads (26 Mar 2026)** — Students "submit" by selecting which portfolio pages to share from their existing unit work. This ties gallery submissions directly to lesson content and prevents gallery from becoming a disconnected upload dump. The snapshot includes page responses, images, and context notes.
- **Open Studio is Mode 1 only, Discovery Journey is Mode 2 only (26 Mar 2026)** — Open Studio (self-directed working mode) is for Design classes (Mode 1). Discovery Journey (project-finding exploration) is for Service/PP/PYPx classes (Mode 2). They are mutually exclusive on the student dashboard — a unit card shows one or the other based on subject detection, never both.
- **No lucide-react in project (26 Mar 2026)** — StudioLoom does not use lucide-react. All icons must be inline SVGs. This was discovered when gallery components caused Vercel build failures. Pattern: create small inline SVG components at the top of the file (e.g., `const XIcon = ({ size = 24 }) => <svg>...</svg>`).
- **Student onboarding as character creation, not survey (27 Mar 2026)** — Students aged 11-16 respond to "set up your design studio" much better than "complete your profile." The 4-screen flow (mentor → theme → guided questions → reveal) borrows from game character creation. Mentor choice + theme choice happen BEFORE any questions — students invest in their identity first, then the mentor asks questions conversationally with animated reactions. Skip option available after essentials (mentor + theme).
- **3 mentors, not 5 (27 Mar 2026)** — The old 5-mentor system (Kit/Sage/River/Spark/Haven) was reduced to 3 (Kit/Sage/Spark) for the onboarding. Each has: distinct AI personality via `aiPromptModifier` injected into Design Assistant system prompt, unique reactions to every profile answer, accent color + gradient. Kit = warm workshop buddy (purple), Sage = calm thinking partner (blue), Spark = bold creative disruptor (orange).
- **Theme system via CSS custom properties, not Tailwind classes (27 Mar 2026)** — 4 visual themes (Clean/Bold/Warm/Dark) applied as `--st-*` CSS custom properties on the student layout root div. This allows runtime theme changes without recompilation. Components use `var(--st-bg)`, `var(--st-accent)`, etc. `getThemeStyles()` returns a `Record<string, string>` of all tokens.
- **Onboarding triggered by null mentor_id, not separate flag (27 Mar 2026)** — `mentor_id === null` on the students table means "hasn't completed onboarding." No separate `onboarding_complete` column needed. The session route returns `mentor_id` in the response; the layout checks `"mentor_id" in data.student && data.student.mentor_id === null` (safely ignores pre-migration schemas where the column doesn't exist).
- **Settings cog for revisiting, not re-triggering onboarding (27 Mar 2026)** — Gear icon in student header opens a quick modal with mentor cards + theme cards for instant switching (PATCH to studio-preferences API). Also has "Redo full Studio Setup" button for the full 4-screen flow. Changes apply immediately — no page reload needed.
- **Student login needs 3-level fallback (27 Mar 2026)** — After migration 041 (class_students junction), login breaks if: (a) junction query fails silently (column doesn't exist), (b) student's class_id was nulled during backfill, (c) student exists but isn't in junction table. Three fallbacks: junction table → legacy class_id → username + author_teacher_id match. The third fallback also re-links the student to the class for future logins.
- **Always include new DB columns in session response JSON (27 Mar 2026)** — Adding columns to the SELECT query in the session route is only half the fix. Must ALSO add them to the JSON response object. The onboarding check failed because `mentor_id` was selected from DB but not included in the response body — `"mentor_id" in data.student` was false.
- **Parallel criterion generation over sequential (27 Mar 2026)** — The wizard was generating criteria A, B, C, D one after the other (~45s each = 3+ min). Switching to `Promise.allSettled` fires all simultaneously (~45s total). Same pattern applied to RAG retrieval within each criterion (`retrieveContext` + `retrieveLessonProfiles` now parallel). The streaming UI still works — each criterion independently updates its own status via dispatch. Key insight: when multiple independent AI calls share no dependencies, always parallelize. The sequential loop was just the simplest initial implementation, not a deliberate design choice.
- **Express mode uses timeline path, not criterion path (27 Mar 2026)** — Express mode sets `journeyMode=true` (the default), which routes to the timeline generation path: skeleton → per-lesson generation with concurrency pool. The criterion-based parallel generation (Phase 0-2 work) only fires when `journeyMode=false` AND no timeline outline selected. The initial parallelization speedup didn't affect Express mode because it was optimizing the wrong code path. Fix: increase per-lesson concurrency from 2→4 in the timeline path, and parallelize RAG lookups within each per-lesson prompt builder (`buildRAGPerLessonPrompt` and `buildRAGTimelinePrompt` now use `Promise.allSettled` for activity cards + RAG context + lesson profiles + teacher style).
- **Dimmed always-visible UI pattern for progressive disclosure (27 Mar 2026)** — Rather than hiding the lane selector until topic is entered (which confused users who didn't know it existed), show it at `opacity-40 pointer-events-none` with a "Next: choose your approach" label. When the prerequisite is met, fade to full opacity and enable interaction. This previews the upcoming step without creating clutter. Better than both hiding (user doesn't know what's next) and enabling prematurely (user clicks before ready).
- **Unit Generation Project: 4-dimension model (27 Mar 2026)** — Unit Type × Programme × Curriculum × Standards are 4 independent, composable dimensions. Unit type lives on the unit (inherent to content — "Design a Chair" is always design). Programme lives on the class (assessment framework — same unit can be MYP in one class, GCSE in another). Curriculum is free-text for now, structured standards database later (Phase 4+). Standards are tagged to individual activities post-generation. Full spec: `docs/specs/unit-type-framework-architecture.md`.
- **3-lane wizard: Express / Guided / Architect (27 Mar 2026)** — Teachers self-select their control level. Express = 3 clicks (AI infers everything from topic text). Guided = 5-7 steps (current wizard enhanced). Architect = single scrollable form, all fields visible. Answers carry forward between lanes — switching doesn't lose data. The lane UI is Phase 3; Phases 0-2 make the underlying data work for all types first. Full spec: `docs/specs/wizard-lanes-spec.md`.
- **CriterionKey must be `string`, not literal union (27 Mar 2026)** — The old `"A" | "B" | "C" | "D"` type couldn't represent GCSE AO1-AO5, Service IPARD, or any non-4-criterion system. Changed to `string` with per-type criteria definitions (`getCriteriaForType(unitType)`). All code that accessed `CRITERIA[key]` directly must use `getCriterion(key, unitType)` instead — the criterion definitions are type-specific.
- **Intelligence Profiles: 3 JSONB columns, incrementally updated (27 Mar 2026)** — Student Intelligence Profile (on students table), Teacher Intelligence Profile (on teachers table), Class Climate Profile (on classes table). Updated at write time using exponential moving average (α=0.3), NOT recomputed at read time. AI reads 200-300 tokens of natural language summary built from the profile. Phase 3/4 feature — raw data collection is already in place (toolkit sessions, pace feedback, discovery sessions, integrity signals). Spec in `docs/roadmap.md` → Intelligence Profile System.

## Lessons Learned (CRITICAL — read before batch operations)

### 1. NEVER batch-modify JSX structure with regex/sed/perl
The Framer Motion disaster (18-19 Mar 2026) destroyed 23 tool components. What happened:
- Batch agent added `<motion.div>` by replacing `<div>` in opening tags
- Closing `</motion.div>` was placed on INNER divs instead of outermost containers
- Attempting to fix with `sed`/`perl` regex deleted opening `<div>` lines that contained animation attributes
- This cascaded into orphaned `</div>` tags, breaking JSX nesting across ALL files
- TypeScript's basic parser (`createSourceFile`) said "OK" but Next.js SWC/Webpack caught real JSX errors
- **LESSON:** Never use regex to modify JSX tag structure. Either rewrite the full component or use the Edit tool for surgical, verified changes.

### 2. Always use `tsc --noEmit` not just `createSourceFile` for JSX validation
`ts.createSourceFile()` only checks syntax — it misses:
- Unclosed JSX elements
- Mismatched opening/closing tags
- Invalid attributes on HTML elements (e.g., `hover` in inline styles)
Run `npx tsc --noEmit --jsx react-jsx --skipLibCheck <file>` to catch real JSX errors.

### 3. Client components cannot import server-only modules
`createAdminClient()` (which reads `SUPABASE_SERVICE_ROLE_KEY`) must NEVER be imported in `"use client"` components. The `onUnitCreated` signal was originally imported in a client page — moved to server-side API route.

### 4. Student auth uses token sessions, not Supabase Auth
Students authenticate via `SESSION_COOKIE_NAME` cookie → `student_sessions` table → `student_id`. NOT via `supabase.auth.getUser()`. The Design Assistant route had this bug — was using teacher auth pattern for students.

### 5. Framer Motion must be installed in the project directory, not home
`npm install framer-motion` in `~` instead of `~/CWORK/questerra` created a conflicting `package-lock.json` that made Next.js infer the wrong workspace root.

### 6. Duplicate tool names in tools-data.ts cause React key warnings
When adding interactive versions of existing template tools, either remove the template entry or rename it with "(Template)" suffix. React keys must be unique.

### 7. School profile belongs in Teacher Settings, not AI controls
School facts (period length, workshop access, doubles) are set once and rarely change. They belong in the teacher settings page, not alongside the AI tuning dials. The AI reads them from the teacher profile at generation time.

### 8. Timing model must be a learning system, not fixed values
Hard-coded timing limits ("max 15 min for direct instruction") are the wrong approach. The system should learn from teacher uploads, edits, and feedback. Cold-start defaults exist only for day-one teachers with no data.

### 9. Student-facing components must use student auth routes
The pace feedback pulse was mounted on a student page but called a teacher auth endpoint (`requireTeacherAuth`), causing 401s. Any API call from a student-rendered component MUST use `/api/student/*` routes with `requireStudentAuth`. Student auth = cookie token session (`SESSION_COOKIE_NAME`), NOT Supabase Auth. This is the same bug that hit the Design Assistant (Lesson Learned #4) — it keeps recurring because it's easy to copy teacher route patterns.

### 10. Admin AI controls need macro/micro hierarchy
50+ individual sliders are engineer-facing, not teacher-facing. Teachers think in terms of "more workshop, less theory" — not "set scaffoldingFade to 7." The macro dials (5 big concepts) → micro sliders (50+ detailed controls) hierarchy serves both audiences.

### 11. Vercel CDN strips Set-Cookie from Cache-Control: public responses
Next.js Route Handlers default to `Cache-Control: public, max-age=0, must-revalidate`. Vercel CDN sees "public" and strips the `Set-Cookie` header from the response — the cookie never reaches the browser. **Any route that sets cookies (login, session, auth) MUST explicitly set `Cache-Control: private, no-cache, no-store, must-revalidate`.** We also added a belt-and-suspenders rule in `next.config.ts` headers for `/api/auth/:path*`. This is Vercel-specific — doesn't happen in local dev, so you won't catch it until production.

### 13. Default vs named exports cause React #130 on Vercel
If a component uses `export default function Foo`, it MUST be imported as `import Foo from "./Foo"` (default import). Using `import { Foo } from "./Foo"` (named import) resolves to `undefined`, causing React error #130 ("Element type is invalid: expected a string or class/function but got undefined"). This works in dev sometimes due to HMR but fails on Vercel production builds. **Rule:** Always check whether a component uses `export default` or `export { Foo }` before writing the import. The barrel file (`index.ts`) re-exports tell you: `export { default as Foo } from "./Foo"` = default export, `export { Foo } from "./Foo"` = named export.

### 14. Always normalize content data before accessing .pages
StudioLoom has 4 content versions: v1 (Record<PageId, PageContent>), v2 (pages array), v3 (journey-based), v4 (timeline). Direct `.pages` access fails silently for v1 (object, not array) and v4 (uses `.timeline`). **Always call `normalizeContentData()` from `@/lib/unit-adapter` before extracting pages.** This converts all versions to a consistent v2-style format with a `.pages` array. The useLessonEditor hook was initially broken because it skipped normalization — content loaded fine but `pages` was undefined for non-v2 units.

### 15. npx tsx can't find project node_modules
`npx tsx scripts/foo.ts` creates an isolated execution environment that can't resolve packages from the project's `node_modules`. **Fix:** Use `./node_modules/.bin/tsx` (requires node_modules to exist) or rewrite as `.mjs` with manual env loading and native Node.js imports. The `.mjs` approach is more portable for scripts that need to run in different environments.

### 16. Project does NOT use lucide-react — all icons must be inline SVGs
lucide-react is not installed in the project. Importing from it causes Vercel build failures (`Module not found: Can't resolve 'lucide-react'`). **Fix:** Create inline SVG components at the top of each file: `const XIcon = ({ size = 24 }: { size?: number }) => (<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">...</svg>);`. This bit us when gallery components were built by agents that assumed lucide-react was available. Check package.json before importing icon libraries.

### 17. Retry insert without new columns for migration-gapped deployments
When adding a new column via migration (e.g., `unit_type` in migration 051) and immediately using it in an insert payload, the insert will 400 on production if the migration hasn't been applied yet. **Fix:** Wrap the insert in a retry that strips the new column(s) from the payload on failure: `if (error && (error.message.includes("unit_type") || error.code === "PGRST204")) { delete payload.unit_type; retry... }`. This gives graceful degradation during the gap between code deploy and migration apply. Applied in `create/page.tsx` `saveUnit()` for `unit_type`.

### 18. Always show save/generation errors to the user
The StickyBuildBar component originally had no error display during the review phase. When `saveUnit()` failed (400 from missing column), the error was dispatched via `SET_ERROR` to state but never rendered — the Save Unit button just did nothing. **Fix:** Pass `error={state.error}` to StickyBuildBar and render a red banner above the action buttons. General rule: any component with an action button must have a visible error state.

### 12. PostgREST PGRST201 ambiguous relationships after adding junction tables
When you add a junction table that creates a second FK path between two existing tables (e.g., `class_students` between `students` and `classes`, alongside the existing `students.class_id` FK), PostgREST can't auto-resolve nested selects like `.select("students(... classes(...))")`. It returns HTTP 300 with a PGRST201 error listing both relationship paths. **Fix options:** (a) use PostgREST `!fk_name` hint syntax to disambiguate (e.g., `.select("classes!class_students(id, name)")`), or (b) query tables separately with no nested joins (what we did — simpler, no Supabase SDK version dependency). This will happen every time a junction table duplicates an existing direct FK relationship. Plan for it when writing migration 041-style additive junction tables.

---

## AI Brain — READ THESE BEFORE ANY AI WORK

**This is the intelligence system that makes StudioLoom more than a template generator.** These documents define how the AI thinks about design teaching. Read the relevant ones before touching any AI-related code.

| Document | Purpose | When to read |
|----------|---------|-------------|
| **`docs/ai-intelligence-architecture.md`** | Master architecture: source-aware ingestion (Pass 0), timing model, 4-layer knowledge system, per-teacher style learning, presentation style, unified intelligence pool | Before ANY AI system changes |
| **`docs/design-teaching-corpus.md`** | Layer 1: What great design teaching looks like — 10 sections covering non-linear design cycle, lesson phases, gradual release, workshop management, Perkins' whole game, assessment, studio culture, critique protocols (Ron Berger), differentiation, technology integration | Before changing AI prompts, lesson generation, student mentoring, or educational content |
| **`docs/timing-reference.md`** | Learning-based timing model — cold-start defaults that get replaced by real data from teacher uploads, edits, and feedback. Usable time formula. Activity duration ranges. Cognitive load curves. | Before touching timing logic or generation prompts |
| **`docs/education-ai-patterns.md`** | 5 core patterns for student-facing AI: effort-gating, Socratic feedback, staged cognitive load, micro-feedback loops, soft gating. SCAMPER is the reference implementation. | Before building any interactive toolkit tool or student AI interaction |
| **`docs/lesson-timing-research-report.md`** | Lesson timing research & architecture: Workshop Model (4-phase), 1+age rule, period templates, cognitive load boundaries, extension generation, teacher modification UI design, gap analysis, implementation plan | Before changing lesson timing, generation prompts, or building lesson-related UI |
| **`docs/student-learning-intelligence.md`** | Student learning profile architecture — how AI builds per-student understanding over time from conversations, assessments, tool work, and portfolio. 4-phase roadmap. | Before building student profiling, assessment features, or adaptive AI |
| **`docs/design-guidelines.md`** | 36 design principles across 5 categories. Feeds the dashboard Guidelines tab. | Before establishing new UI patterns or making design decisions |
| **`docs/open studio/open-studio-spec.md`** | Open Studio feature spec: unlock criteria, AI mentor behaviour (guided vs studio critic), drift detection, teacher dashboard, implementation notes | Before modifying Open Studio features or AI mode switching |
| **`docs/open studio/open-studio-experience-design.md`** | Open Studio experience journey: 4-phase arc (Discovery → Planning → Working → Sharing), AI-guided discovery conversation, backward planning from end date, multi-channel evidence collection, health score model, community resource library, project archetypes, visual quest journey UX. The CANONICAL guideline for how Open Studio should feel. | Before building any Open Studio UI, AI prompts, or student-facing features |
| **`docs/specs/discovery-engine-build-plan.md`** | **THE master build plan (2,441 lines).** 12 parts: build order, content bible (all content pools), image strategy, state machine, route architecture, gap fills (scoring normalization, Mode 2 redirect, tool dedup, Kit story), group work future spec, dead-end audit (19 response specs), Mode 1 vs Mode 2 template doors (36 total), people icon split scoring, teacher content control panel, revised station weights. Contains ALL Kit dialogue, ALL interaction content, ALL scoring algorithms. | **Before building ANY Discovery Engine component.** This is the single source of truth. |
| **`docs/specs/discovery-engine-spec.md`** | Data model: DiscoveryProfile interface, 6 knowledge domains, station data types, database schema | Before building Discovery data layer or state management |
| **`docs/specs/discovery-engine-ai-integration.md`** | AI touchpoint map: which stations call which models, prompt templates, fallback chains, async pre-generation strategy, cost budget | Before building Discovery AI endpoints |
| **`docs/specs/discovery-engine-ux-design.md`** | Visual design: backgrounds, Kit expressions/animations, station transitions, color palettes, interaction patterns, responsive considerations | Before building Discovery UI components |
| **`docs/research/student-influence-factors.md`** | Hattie-style effect size synthesis: 24 cultural, linguistic, psychological, and environmental factors influencing learning. Top 10 measurable factors, interaction effects, measurement blueprint, implementation roadmap. Research foundation for Student Learning Profile. | Before building student profiling, Discovery Engine scoring, peer grouping, language scaffolding, cultural adaptation, or any feature touching student identity/wellbeing |

**Code that implements the brain:**
- `src/lib/ai/prompts.ts` → `buildDesignTeachingContext()` injects corpus + teacher style + Workshop Model into all generation prompts; `buildTimingBlock()` enforces usable time + 1+age rule + extensions; `maxInstructionMinutes()` computes 1+age cap; grade profiles include `avgStudentAge`
- `src/lib/ai/timing-validation.ts` → `validateLessonTiming()` checks 8 Workshop Model rules with auto-repair; `validateUnitTiming()` for unit-wide checks; `TIMING_PRESETS` with 4 presets; `applyTimingPreset()` for one-click application
- `src/components/lesson-timing/PhaseTimelineBar.tsx` → Drag-to-resize teacher modification UI; `buildDefaultPhases()` factory
- `src/components/lesson-timing/TimingFeedbackPrompt.tsx` → Post-lesson timing feedback collection
- `src/lib/ai/design-assistant-prompt.ts` → Student mentor system prompt with Design Teaching Intelligence (guided mode)
- `src/lib/ai/open-studio-prompt.ts` → Studio critic system prompt with 5 interaction modes (Open Studio mode)
- `src/lib/knowledge/analyse.ts` → Pass 0 classification + 5 type-specific analysis pipelines
- `src/lib/knowledge/analysis-prompts.ts` → All analysis prompts (v2.0.0) with rubric/safety/exemplar/content pipelines
- `src/lib/teacher-style/profile-service.ts` → Teacher style profile CRUD + passive signal collection
- `src/types/teacher-style.ts` → TeacherStyleProfile type + confidence computation
- `supabase/migrations/027_teacher_style_profile.sql` → Database column for style profiles (APPLIED 18 Mar 2026)

## Notes
- Target users: MYP Design teachers and students (ages 11-18), secondary schools
- Pedagogical foundation: Hattie's High-Impact Teaching Strategies (HITS), Richard Paul's 6 question types, Bloom's taxonomy
- Sister project: Makloom (makloom.com) — consumer/individual version for anyone learning DT skills, ~60-70% code reusable
- The audit report at `docs/ai-systems-audit-2026-03.md` has detailed file-by-file findings
- The roadmap at `docs/roadmap.md` (~950 lines) is the comprehensive feature plan
- **Codebase scale (as of March 2026):** ~245 source files, ~62,000 lines of code
- **Haiku model ID:** correct is `claude-haiku-4-5-20251001` — the old ID `claude-haiku-4-5-20250315` returns 404. Was fixed across conversation.ts, usage-tracking.ts, and free tools routes in the original build session.
- **API constraint:** `thinking` + `tool_choice` cannot be used together in the Anthropic API — endpoints using `tool_choice` for structured output must NOT include `thinking` config
- **Shared DNA across Matt's projects:** All of Matt's projects (StudioLoom, Seedlings, CALLED, Makloom) follow the same core pattern: a guided learning/growth experience where a mentor creates structured content and a participant engages with it + AI support. StudioLoom is the most complete template to clone from. Reusable: auth, class management, RAG pipeline, AI integration, gamification, portfolio. Unique per project: domain content, rubrics, role names, visual identity.
