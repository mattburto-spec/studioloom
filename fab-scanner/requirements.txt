# Runtime dependencies for the Fly.io container. Pinned conservatively; the
# sidecar `pyproject.toml` is the source of truth for range constraints.
# Regenerate with:
#   pip-compile --output-file=requirements.txt pyproject.toml
# or manually pin to a known-good resolved set before `fly deploy`.

trimesh==4.11.5
scipy==1.13.1
networkx==3.4.2
lxml==6.1.0
matplotlib==3.9.2
numpy==2.0.2
httpx==0.27.2
structlog==24.4.0
pydantic==2.9.2
supabase==2.9.1
# Phase 2B: SVG path geometry + cairo thumbnail rendering.
# cairocffi needs libcairo2-dev + libpango1.0-dev + pkg-config at build time;
# runtime needs libcairo2 + libpango-1.0-0. See Dockerfile.
svgpathtools==1.7.1
cairocffi==1.7.1
# Phase 2B-6: SVG → PNG thumbnail rendering. cairosvg uses cairocffi +
# libcairo under the hood; the Dockerfile already provides libcairo2
# and libpango-1.0-0 at runtime. Local macOS dev without libcairo falls
# back to thumbnail=None gracefully (see worker/thumbnail_svg.py).
cairosvg==2.7.1
# Phase 2B-5: embedded-raster DPI + transparency inspection (R-SVG-12/13).
# Already installed transitively via matplotlib; made explicit here to
# avoid relying on that chain.
pillow==12.2.0
